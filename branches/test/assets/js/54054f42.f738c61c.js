"use strict";(self.webpackChunkrspamd_docs=self.webpackChunkrspamd_docs||[]).push([[6605],{64035(e){e.exports=JSON.parse('{"version":"4.1.4","date":"2026-07-29","type":"patch","title":"Patch Release Fixing a Controller Authentication Fail-Open, Regexp Engine Resource Bounds, and jemalloc Startup Segfaults","sections":[{"title":"Added","items":["**Read-only access to the Selectors tab**: The `list_extractors`, `list_transforms` and `check_selector` endpoints are now open to read-only users, matching the policy already used for maps \u2014 they only return static catalogs or parse a selector without executing it. `check_message` remains privileged, since it runs a user-supplied selector against a user-supplied message ([#6157](https://github.com/rspamd/rspamd/pull/6157))","**Errors history for read-only users**: The `/errors` endpoint and its WebUI table were gated behind the privileged password. Any authenticated user can now view them: the endpoint returns only Rspamd\'s internal operational error log (timestamp, pid, level, module, message) \u2014 no message content, PII or secrets \u2014 while read-only users already see strictly more sensitive data in the History tab. Authentication is still required and the endpoint mutates nothing ([#6156](https://github.com/rspamd/rspamd/pull/6156))"]},{"title":"Changed","items":["**One shared jemalloc instance per process** (incompatible): The build system searched for `jemalloc_pic` before `jemalloc`, so on Debian it preferred the static `libjemalloc_pic.a` over the shared library and linked it into the common library list used by nine shared objects. Each of them ended up with a complete private allocator holding its own arenas and extent map, so memory obtained through one copy could not be released through another \u2014 and jemalloc-enabled packages segfaulted at startup. CMake now searches for the shared library only, fails the configure step when just a static archive is available (except in fully static builds), and attaches jemalloc per target: `librspamd-server` \u2014 the only code calling `mallctl()` \u2014 plus each executable, pinned with `--no-as-needed` so the linker keeps the otherwise-unreferenced dependency and libc cannot win the `malloc()` lookup. The `malloc_conf` tuning moves into the server library, where it actually takes effect with a shared jemalloc (an executable exports nothing without `-rdynamic`, so it had been silently lost) and now applies to every rspamd binary rather than just the daemon. The jemalloc configuration is also built and tested in CI now, mirroring the shipped Debian/Ubuntu amd64 packages, which had never been covered before ([#6155](https://github.com/rspamd/rspamd/pull/6155), [#6153](https://github.com/rspamd/rspamd/issues/6153))"]},{"title":"Fixed","items":["**PCRE2 match data leaked on invalid UTF input**: `rspamd_regexp_search()` allocates the PCRE2 match data before the JIT UTF-8 validation guard, but the early return on invalid input skipped the free at the function tail, leaking one match-data block (sized to the capture count) per rejected input. The guard is entered by any ordinary regexp \u2014 it tests whether a separate raw pattern was compiled, not whether raw matching was actually selected \u2014 and the regexp cache deliberately routes raw matching of UTF-marked classes through PCRE, so a message with a non-UTF-8 text part reached the leak through the normal scan pipeline, once per such regexp per part","**Heap used by a single regexp match bounded**: PCRE2 match contexts set only the backtracking and depth limits, leaving the heap limit at the build-time default of 20 GB \u2014 effectively no limit. The interpreter grows its backtracking frame vector as frame size times depth, so one non-JIT match on a 60 KB subject with a 2 KB frame could allocate around 485 MB before the depth limit intervened, and far more with a larger frame; this path is taken whenever JIT is unavailable or refused for a pattern. An explicit 64 MiB heap limit is now set, preferring `pcre2_set_depth_limit` over the deprecated recursion-limit call where available. The PCRE1 path had no explicit limits at all \u2014 they live in the `pcre_extra` block, and `pcre_study` legitimately returns NULL when there is nothing to optimise \u2014 so a block is now allocated when needed and both match and recursion limits are set, with a much lower depth since PCRE1 backtracks on the C stack. Two PCRE1 drive-bys: study data is freed unconditionally rather than only in JIT builds, and the raw pattern is now studied for the raw matcher instead of handing it study data from a different compiled pattern","**re:matchn() no longer loops forever on empty matches**: An incremental search resumes from the previous match end, so a zero-width match leaves the cursor where it was. `re:search()`, `re:split()` and the regexp cache loop all guard against this, but `re:matchn()` did not \u2014 with unlimited max matches, a pattern that can match the empty string spun forever and hung the worker. This was reachable from configuration: the spamassassin plugin passes unlimited for any `multiple` rule without `maxhits`, so one SA rule with an empty-matchable pattern wedged the process. The same cursor-progress guard is now applied","**Bounded regexp patterns read past their end when generating ids**: `rspamd_regexp_new_len()` parses within the given pointer and length, but id generation then called `strlen()` on the same pointer. Callers passing a slice of a larger buffer \u2014 the composites expression parser and the symcache delayed symbols \u2014 read past the end of the pattern, a heap-buffer-overflow under ASAN whenever the slice is not NUL-terminated. The identity was wrong even when the read stayed in bounds: the same regexp got different ids as a slice and as a standalone string, while two different slices sharing a start address got the same id and collided in the regexp cache, which compares ids only. The length is now passed down to id generation, and three error paths that printed the raw pointer are bounded as well. Ids produced by `rspamd_regexp_new()` and the regexp cache are unchanged","**Regexp data limit propagated to named scopes**: `regexp.max_size` was applied to the default scope only, so regexps registered in a named scope \u2014 multimap `regexp_rules`, for instance \u2014 were matched against unbounded input while the default scope was capped at 1 MiB. Named scope caches are now created inheriting the default scope limit, and setting the limit updates every scope registered so far, which covers both orderings: Lua scopes registered before the regexp module is configured, and plugins registering scopes during filters init","**Lua URL filter consulted at most twice per user field**: Once the user field of a URL exceeded `max_email_user`, the parser consulted the Lua URL filter on every subsequent byte, and each call re-scanned the whole prefix \u2014 control characters, UTF-8 validation, `@` counting, custom filters. The 512-byte user limit in Lua never applied here because the mid-parse fragment contains no `@` yet, so this ran up to the 16 KB threshold: quadratic scanning plus thousands of C-to-Lua transitions, turning a single 16 KB URL into ~0.6 s of CPU. The filter is now consulted at most twice per user field \u2014 once when the limit is first exceeded, and once when the field ends at `:` or `@` so custom filters still see the complete userinfo. Parsing results are unchanged; the same URL now takes about a millisecond","**Words retained for a message bounded by a message-wide budget**: The UTF tokenizer marked decayed words as skipped but still added them to the words vector, unlike the raw tokenizer which drops them \u2014 so the decay bounded nothing. Every word of a text part was retained at 72 bytes each and then normalized and stemmed with several pool allocations per word: a single 5 MB text part of 500k words cost 34 MiB of word vectors plus 27 MiB of pool allocations where the decay is meant to keep around 1200 words. The 200 ms tokenization guard did not help either, since it was only enabled for parts above 1 MiB, so a message of many sub-megabyte parts could accumulate huge word vectors within the 50 MiB message limit. Decayed words are now dropped as the raw tokenizer does; a message-wide budget (100k words, 1 MiB of text) is shared by all text parts and the meta words, with parts that hit it flagged as truncated and the message logged once; the time checks also trigger on the number of produced words; and skipped words are no longer normalized or stemmed. Note that decayed words of UTF parts are no longer visible to word regexp classes and part similarity hashes, which matches the long-standing raw tokenizer behaviour","**CSS tokeniser stack overflow on sequential comments**: `next_token()` called itself after each comment, so an input made of many comments in a row consumed stack proportional to their count. The parser\'s own recursion limit does not cover this path, and the self-call is only elided when the compiler applies the tail call \u2014 a debug or ASAN build keeps a real call and overflows the stack. The tokenisation loop is now iterative, continuing from the offset past each comment, with doctest coverage for leading, trailing, interleaved, nested and unterminated comments plus a 100k-comment stress case","**actrie startup segfault with jemalloc**: `acism_create()` allocated its buffers with GLib but released them with libc `free()`, which only works while both resolve to the same allocator. With the duplicated per-object jemalloc copies described above, `free()` inside the actrie library ran one jemalloc copy\'s deallocator on a pointer owned by whichever allocator GLib had bound to, segfaulting inside `rspamd_url_init()` during startup. The whole lifecycle now stays inside GLib, so the pair resolves to one allocator no matter how symbol interposition falls out ([#6153](https://github.com/rspamd/rspamd/issues/6153))","**rspamadm resolves SRV-based upstreams**: rspamadm never configured the upstreams library, so the upstreams context had neither an event loop nor a resolver. Plain host names still worked, as they resolve synchronously during config parsing, but the SRV form `servers = \\"service=fuzzy+example.com\\"` creates a placeholder that is only selectable after asynchronous resolution \u2014 which was never scheduled. Every fuzzy storage configured that way looked empty: `rspamadm fuzzy_ping -l` printed no servers and pinging failed with \\"no fuzzy storage upstream available\\". The library is now configured right after the resolver is created, before any command loads its configuration, and since one-shot commands issue their first request before the resolve timer fires, a new `lua_fuzzy.wait_for_storages()` \u2014 used by `fuzzy_ping` and `fuzzy_hash` \u2014 waits for the selected rules to have servers, bounded by the request timeout; an explicit `-s`/`--server` override skips the wait as it bypasses the configured upstreams. A ping that cannot start now also reports the rule name instead of nil"]},{"title":"Security","items":["**Controller fails closed on a malformed password hash**: `rspamd_is_encrypted_password()` validated only the `$<id>` prefix of a configured password, not that a salt and a key follow it. A hash such as `$1$` therefore passed the check, reached the verification with both components absent, skipped the comparison block entirely and returned its initial value of TRUE \u2014 any supplied password authenticated, and was then cached as the valid one. The result is now initialised to FALSE and set only after a completed KDF comparison, and the malformed hash is logged instead of silently accepted. The component extraction also indexed three bytes into the string without checking its length, reading past the terminator for short inputs and making the fail-open depend on adjacent heap bytes; it now takes the length and bails out when a component is missing. The startup sanity check re-ran the same prefix-only test, so a malformed password also passed configtest silently \u2014 it now decodes the salt and the key and validates their lengths against the pbkdf matching the hash id, which additionally fixes a hardcoded reference to the first pbkdf in the list"]}],"summary":"This release closes a critical controller authentication fail-open \u2014 a malformed password hash such as `$1$` accepted any password, and configtest did not catch it \u2014 and fixes the startup segfault in jemalloc-enabled packages by linking one shared allocator instance per process instead of a private copy per shared object, a configuration that is now also exercised in CI. The regexp engine gets a broad hardening pass: an explicit heap bound for a single match on both PCRE2 and PCRE1, a match-data leak on invalid UTF input reachable from the normal scan pipeline, an infinite loop in `re:matchn()` on empty-matchable patterns, an out-of-bounds read when generating ids for bounded patterns, and the data limit finally applied to named scopes. Rounding it out: quadratic URL parsing with per-byte Lua filter calls is gone, tokenizer word retention is bounded by a message-wide budget, the CSS tokeniser no longer overflows the stack on comment floods, and rspamadm handles SRV-based fuzzy upstreams. Recommended upgrade for all users, and strongly recommended for anyone running the controller with worker passwords."}')}}]);