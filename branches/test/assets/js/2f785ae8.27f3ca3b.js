"use strict";(self.webpackChunkrspamd_docs=self.webpackChunkrspamd_docs||[]).push([[4167],{84992(e,t,a){a.r(t),a.d(t,{default:()=>V});var s=a(96540),r=a(2590),i=a(28774),o=a(60023);const n="releaseCard_V_vn",d="releaseHeader_WFii",l="releaseInfo_lG6c",c="versionSection_EhC2",p="releaseType_XlIj",h="version_Ivo7",m="versionLink_JPQG",u="date_lDu3",g="titleSection_q8Wx",f="title_LYM2",b="description_JBV_",y="expandButton_s8_L",w="arrow_eh1w",v="expanded_iFmC",k="releaseContent_YOuB",x="changes_M4J2",_="changeSection_q0vS",T="sectionTitle_dPiS",R="sectionIcon_lHvv",A="changeList_xLVo",S="changeItem_fQM5";var z=a(74848);function C(e){var t,a=e.release,r=e.typeInfo,C=(0,s.useState)(!1),L=C[0],M=C[1],P="version-"+a.version.replace(/\./g,"-");return(0,z.jsxs)("div",{className:n,id:P,children:[(0,z.jsxs)("div",{className:d,onClick:function(){return M(!L)},children:[(0,z.jsxs)("div",{className:l,children:[(0,z.jsxs)("div",{className:c,children:[(0,z.jsxs)("span",{className:p,style:{backgroundColor:r.color},children:[r.icon," ",r.label]}),(0,z.jsx)("h2",{className:h,children:(0,z.jsxs)(i.A,{to:"/changelog/"+a.version,className:m,onClick:function(e){return e.stopPropagation()},children:["v",a.version]})}),(0,z.jsx)("span",{className:u,children:(t=a.date,new Date(t).toLocaleDateString("en-US",{year:"numeric",month:"long",day:"numeric"}))})]}),(0,z.jsxs)("div",{className:g,children:[(0,z.jsx)("h3",{className:f,children:a.title}),(0,z.jsx)("p",{className:b,children:r.description})]})]}),(0,z.jsx)("div",{className:y,children:(0,z.jsx)("span",{className:w+" "+(L?v:""),children:"\u25bc"})})]}),L&&(0,z.jsx)("div",{className:k,children:(0,z.jsx)("div",{className:x,children:a.sections.map(function(e){return function(e,t){if(!t||0===t.length)return null;var a=function(e){switch(e.toLowerCase()){case"breaking changes":return{color:"#dc3545",icon:"\ud83d\udca5"};case"added":return{color:"#27ae60",icon:"\u2705"};case"changed":case"improved":return{color:"#3498db",icon:"\ud83d\udd04"};case"fixed":return{color:"#e67e22",icon:"\ud83d\udd27"};case"security":return{color:"#e74c3c",icon:"\ud83d\udee1\ufe0f"};case"deprecated":return{color:"#f39c12",icon:"\u26a0\ufe0f"};case"removed":return{color:"#95a5a6",icon:"\u274c"};default:return{color:"#7f8c8d",icon:"\ud83d\udcdd"}}}(e);return(0,z.jsxs)("div",{className:_,children:[(0,z.jsxs)("h4",{className:T,style:{color:a.color},children:[(0,z.jsx)("span",{className:R,children:a.icon}),e]}),(0,z.jsx)("ul",{className:A,children:t.map(function(e,t){return(0,z.jsx)("li",{className:S,dangerouslySetInnerHTML:{__html:(a=e,o.xI.setOptions({breaks:!1,gfm:!0}),o.xI.parse(a).replace(/^<p>|<\/p>$/g,""))}},t);var a})})]},e)}(e.title,e.items)})})})]})}const L="rssLinks_WwUv",M="rssLink_hiVh",P="rssIcon_S3OC";function I(){return(0,z.jsxs)("div",{className:L,children:[(0,z.jsxs)("a",{href:"/rss/changelog.xml",className:M,title:"Subscribe to Rspamd Releases RSS Feed",target:"_blank",rel:"noopener noreferrer",children:[(0,z.jsx)("svg",{className:P,xmlns:"http://www.w3.org/2000/svg",viewBox:"0 0 24 24",fill:"currentColor",children:(0,z.jsx)("path",{d:"M6.18 15.64a2.18 2.18 0 0 1 2.18 2.18C8.36 19 7.38 20 6.18 20C5 20 4 19 4 17.82a2.18 2.18 0 0 1 2.18-2.18M4 4.44A15.56 15.56 0 0 1 19.56 20h-2.83A12.73 12.73 0 0 0 4 7.27V4.44m0 5.66a9.9 9.9 0 0 1 9.9 9.9h-2.83A7.07 7.07 0 0 0 4 12.93V10.1z"})}),"Changelog RSS"]}),(0,z.jsxs)("a",{href:"/blog/rss.xml",className:M,title:"Subscribe to Rspamd Blog RSS Feed",target:"_blank",rel:"noopener noreferrer",children:[(0,z.jsx)("svg",{className:P,xmlns:"http://www.w3.org/2000/svg",viewBox:"0 0 24 24",fill:"currentColor",children:(0,z.jsx)("path",{d:"M6.18 15.64a2.18 2.18 0 0 1 2.18 2.18C8.36 19 7.38 20 6.18 20C5 20 4 19 4 17.82a2.18 2.18 0 0 1 2.18-2.18M4 4.44A15.56 15.56 0 0 1 19.56 20h-2.83A12.73 12.73 0 0 0 4 7.27V4.44m0 5.66a9.9 9.9 0 0 1 9.9 9.9h-2.83A7.07 7.07 0 0 0 4 12.93V10.1z"})}),"Blog RSS"]}),(0,z.jsxs)("a",{href:"/blog/atom.xml",className:M,title:"Subscribe to Rspamd Blog Atom Feed",target:"_blank",rel:"noopener noreferrer",children:[(0,z.jsx)("svg",{className:P,xmlns:"http://www.w3.org/2000/svg",viewBox:"0 0 24 24",fill:"currentColor",children:(0,z.jsx)("path",{d:"M6.18 15.64a2.18 2.18 0 0 1 2.18 2.18C8.36 19 7.38 20 6.18 20C5 20 4 19 4 17.82a2.18 2.18 0 0 1 2.18-2.18M4 4.44A15.56 15.56 0 0 1 19.56 20h-2.83A12.73 12.73 0 0 0 4 7.27V4.44m0 5.66a9.9 9.9 0 0 1 9.9 9.9h-2.83A7.07 7.07 0 0 0 4 12.93V10.1z"})}),"Blog Atom"]})]})}const F="changelog_D9XG",D="header_kmYz",E="subtitle_aupl",U="rssSection_q8j7",N="filters_hJKK",H="filterButton_yd6U",j="active_us8t",B="releases_ZDc6",q="noReleases_A1BR";var O=[{version:"4.1.5",date:"2026-08-14",type:"patch",title:"Patch Release Gating File and Shared-Memory Message Sources, with Fuzzy Sender Facts and Font-Aware PDF Text Extraction",sections:[{title:"Added",items:['**Fuzzy sender facts shared with storages**: Rules that share extensions with their storage now attach a sender-facts extension to check commands sent over encrypted connections: a single 32-bit word carrying the SPF, DKIM and DMARC results, the PTR state \u2014 including the generic/dynamic hostname verdict that hfilter now publishes for other modules to reuse instead of duplicating its pattern set \u2014 a bucketed recipient count, and whether the sending client used TLS. Authentication results separate "this address is the source" from "this address relayed or forged", which is exactly the distinction an IP reputation model built from fuzzy telemetry has been missing. Every field describes the sender and never the recipient or the local policy: recipient counts are bucketed rather than exact, and the client\'s own verdict, action and score are deliberately absent, since they would create a feedback loop with the reputation list. The values come from the checks themselves rather than from symbol names, so the SPF and DMARC modules now publish their policy results in the task mempool, and `FUZZY_CALLBACK` gains ordering dependencies so the results exist by the time commands are built ([#6179](https://github.com/rspamd/rspamd/pull/6179))',"**Font-aware PDF simple font text decoding**: A string in a PDF content stream holds character codes, not characters \u2014 byte 0xe9 is Oslash in StandardEncoding, e-acute in WinAnsiEncoding and Egrave in MacRomanEncoding \u2014 so without the /Encoding of the font that drew it the text cannot be decoded, and running charset detection over raw codes is guesswork. A new libmime unit owns the three Annex D base encodings and per-font /Differences overrides, plus a builder that turns a page worth of text runs into a single UTF-8 buffer with no per-run allocation and unescapes literal and hex strings in one pass instead of a chain of Lua gsubs. The extractor now keeps the font selected by each Tf operator, resolves every font of a page to an encoding, and replays the whole page through one decoder; quality filtering is scored per page, where the statistics mean something, rather than per text operator ([#6175](https://github.com/rspamd/rspamd/pull/6175))","**PDF /ToUnicode CMaps and composite fonts**: A composite (Type0) font addresses glyphs by index, so its character codes mean nothing on their own and no encoding table can recover the text \u2014 the only thing that can is the /ToUnicode CMap the producer embedded, which is now parsed: codespace ranges give the code widths, bfchar maps single codes and bfrange maps runs, with UTF-16BE destinations that may expand to several characters or a surrogate pair. Every table is capped, because a CMap comes from the message. A font's CMap takes precedence over /Encoding \u2014 its codes are glyph indices, so an encoding table would only invent letters \u2014 codes the CMap does not map produce nothing rather than leaking a glyph index into the text, and the UTF-16 heuristic now applies only when no CMap is authoritative, since composite fonts emit exactly the zero-interleaved byte pattern it looks for and it used to hijack the very strings a CMap is meant to decode ([#6175](https://github.com/rspamd/rspamd/pull/6175))","**Structured fuzzy results and message size in the Elastic schema**: The elastic plugin replaces the flat `fuzzy_hashes` list with per-match objects from `task:get_fuzzy_results()` \u2014 rule, symbol, found and queried hashes, type, probability, flag and exactness \u2014 with a `fuzzy_nested` option to toggle the nested type, and adds the message size to the schema mapping so its type is declared rather than inferred per index ([#6154](https://github.com/rspamd/rspamd/pull/6154), [#6136](https://github.com/rspamd/rspamd/issues/6136))"]},{title:"Changed",items:["**GPT condition callback returns only a boolean** (incompatible): The `condition` option was previously expected to return both the decision and the content for the LLM request, a contract that was non-obvious and only discoverable through debugging. The callback now returns only true/false plus an optional reason, and request content generation has moved into `gpt_check` itself, making custom conditions much simpler to write. Deployments whose condition returned request content must update it to return the boolean decision alone ([#6159](https://github.com/rspamd/rspamd/pull/6159), [#5857](https://github.com/rspamd/rspamd/issues/5857))","**Explicit `allow_file_and_shm_inputs` defaults shipped**: The normal, controller and proxy worker configurations now set `allow_file_and_shm_inputs = true` explicitly, with a comment explaining what the option permits, that it applies to TCP listeners only (unix sockets always allow these inputs), and that a listener with it enabled must not be exposed to untrusted networks \u2014 authentication and encryption do not authorize the capability. No shipped default value changes in this release, so existing deployments keep working; the default becomes false in the next major release, so operators relying on file or shared-memory inputs over TCP should set the option deliberately now. Commented `max_connections` examples are also added to the controller and proxy ([#6162](https://github.com/rspamd/rspamd/pull/6162))"]},{title:"Fixed",items:['**Per-key fuzzy `forbidden_ids` replaces the default set**: The v2 reply path checked the global `default_forbidden_ids` first and suppressed matching flags unconditionally, letting a per-key set only add further restrictions. That inverted the semantics: a key carrying its own `forbidden_ids` \u2014 including an explicitly empty one, which means "nothing is forbidden for this key" \u2014 is supposed to replace the global default, which exists only for keys with no ACL of their own. In production this meant a storage-wide `forbidden_ids = [8]` suppressed flag 8 for every client, including keys whose own list was empty, so that feed was not served at all. The per-key set alone is now checked whenever the session is encrypted and the key has one, with the global list as the fallback',"**Per-key ACL applied to delayed v1 replies**: The v1 reply path gated the per-key check on a positive match probability, but the DELAY branch zeroes the probability while leaving the flag intact, so a delayed reply fell through to the global default and a key silently lost its own ACL \u2014 the same inversion as in the v2 path, limited to encrypted delayed replies. The probability check is dropped; outside DELAY nothing changes, since a zero probability means a miss with no flag to filter","**Unknown fuzzy wire extensions are skipped, not fatal**: The extensions TLV was not self-describing \u2014 an entry's length was implied by its type \u2014 so a storage could not skip a type it did not know and rejected the whole command, meaning the client got no fuzzy answer at all rather than merely losing the extra field, and every new extension type would have broken every storage predating it. Type bytes from 0x80 up are now always followed by a length byte and can be skipped when unknown, the sender-facts extension is reserved in that range, and the parser was consolidated so a malformed packet from the network can no longer reach an assertion that aborts the daemon ([#6179](https://github.com/rspamd/rspamd/pull/6179))","**No fuzzy extensions for own senders or plain-text rules**: The sender address extension was shipped whenever a rule did not set `no_share`, so deployments running fuzzy checks on outbound or authenticated submission traffic sent their own users' IP addresses to public fuzzy servers. Extensions are now omitted entirely for authenticated tasks and sources in local networks \u2014 such addresses are noise in a reputation model and exactly the values that had to be filtered downstream anyway \u2014 and, as the code already claimed but did not enforce, for rules that talk to their storage in plain text, where the data would travel in the clear ([#6179](https://github.com/rspamd/rspamd/pull/6179))",'**static_embed word extraction bounded**: The static_embed neural provider embedded every word of a message, so a multi-megabyte text part became a million-entry table plus a mean-pool over every token \u2014 about 200 ms per megabyte inside a synchronous rule, observed as multi-second event-loop stalls with "slow synchronous rule" warnings \u2014 and the subject handling compounded it by prepending words one at a time, shifting the whole table per word. Word extraction now honours `max_words` as a total cap that the provider passes through (the knob `fasttext_embed` already had), with subject words seeding the table first and body words appending up to the cap. The cap changes long-text vectors, so training and inference must agree on it ([#6187](https://github.com/rspamd/rspamd/pull/6187))',"**Neural fusion survives a missing provider**: The fusion loop iterated the collected feature vectors with `ipairs`, which stops at the first hole in the table \u2014 one failed or unregistered provider in an early slot silently discarded every later provider's collected block, and downstream fusion consumers went dark with no diagnostic at all. The loop now covers the whole provider slot range including the trailing metatokens slot, so a missing provider yields a partial fused vector that dimension checks can actually see and report ([#6186](https://github.com/rspamd/rspamd/pull/6186))",'**Phantom slow-rule warnings from the symcache**: A rule\'s start time was stamped only on profiled tasks, but the elapsed time was measured whenever the task was profiled or the rule had ever been flagged slow. Once a rule had been flagged slow once, every subsequent non-profiled task measured it against a start of zero \u2014 the task start \u2014 so any DNS-bound or deadline-hitting scan logged a multi-second "slow synchronous rule" against a callback that actually ran in sub-millisecond time; one production scanner saw around 1200 phantom 5\u20138 s warnings per day. The stamp is now taken under the same condition as the measurement ([#6188](https://github.com/rspamd/rspamd/pull/6188))','**HFILTER_HELO_IP_A compares HELO addresses against the connecting IP**: The symbol is documented as "Helo A IP != hostname IP", but the comparison sat in a branch reached only when both the A and AAAA lookups had failed \u2014 where the resolved-address list is necessarily empty \u2014 so it fired if and only if the HELO host resolved to nothing, a condition already covered by HFILTER_HELO_NORES_A_OR_MX, and never on the mismatch it describes. Because the two lookups complete in arbitrary order, the fix joins on an explicit completion counter and judges once both callbacks have returned, keeping the MX fallback on the both-failed path and ignoring transient lookup failures rather than treating them as a mismatch. Functional coverage is added for a plugin that previously had none ([#6176](https://github.com/rspamd/rspamd/pull/6176), [#6165](https://github.com/rspamd/rspamd/issues/6165))','**Schemeless email hrefs no longer fire URL_USER_PASSWORD**: When guessing the missing scheme of an HTML href, only a leading `@` selected the `mailto:` prefix, so `<a href="user.name@example.com">` \u2014 where the first non-alphanumeric character is the dot in the local part \u2014 kept the `http://` prefix and produced `http://user.name@example.com` with a userinfo component, firing URL_USER_PASSWORD (2.0 points) on legitimate messages; nodemailer emits exactly such quote-attribution links. Characters legal both in a hostname and in an email local part (`.`, `-`, `_`, `+`) are now treated as ambiguous and scanning continues until a genuinely structural character decides, so `javascript:`/`data:` hrefs, relative paths and bare hostnames all keep their previous handling','**`get_urls_filtered()` no longer drops flagless URLs**: When called without an include list, the function sets its mask to "all flags", but the include test was a plain bitwise and against that mask \u2014 which a URL carrying no flags at all still fails, and URLs extracted from HTML parts carry no flags. The no-argument form, which asks for everything, silently returned nothing for HTML-only messages. The include filter now applies only when a list was actually given: `rspamadm mime urls` shows HTML URLs again, and URI_COUNT_ODD no longer runs its parity check on an undercount for exactly the multipart/alternative messages it targets ([#6182](https://github.com/rspamd/rspamd/pull/6182), [#5891](https://github.com/rspamd/rspamd/issues/5891))',"**MIME rewriters unwind every boundary level**: `add_text_footer()`, `remove_attachments()` and `multipattern_text_replace()` closed a fixed number of boundaries when the part walk returned from a nested part to a shallower one, rather than however many levels were actually left behind. As soon as a message nested deeper than the hardcoded count, the boundary stack desynchronised: closing delimiters landed in the wrong places, turning attachments into an unreachable epilogue in the footer case and leaking literal `--boundary--` text into part bodies in the others \u2014 reproducible through `rspamadm mime strip` on a triply-nested message. All three now unwind to the boundary being re-entered, which also subsumes an earlier multipart/related special case, with unit tests over flat and one- to three-level nesting that fail on the unpatched code ([#6173](https://github.com/rspamd/rspamd/pull/6173), [#6178](https://github.com/rspamd/rspamd/pull/6178), [#6170](https://github.com/rspamd/rspamd/issues/6170))","**TJ positioning gaps kept out of the PDF character code stream**: The numbers in a TJ array are positioning adjustments, not character codes, but a large negative one was turned into a space and concatenated with the strings around it. For a simple font that only made an ASCII space arrive by the wrong route; for a composite font it inserted one byte into a stream of two-byte codes, so every code after the gap was read across a boundary. The pieces of a text operator now travel separately and the gap is appended structurally, where it cannot disturb the framing ([#6175](https://github.com/rspamd/rspamd/pull/6175))","**Inline PDF /Resources dictionaries honoured**: Only the indirect form of a page's /Resources was unwrapped; a /Resources written out as an inline dictionary \u2014 the common case \u2014 was replaced by an empty table, so everything a page declares, fonts above all, was invisible to extraction ([#6175](https://github.com/rspamd/rspamd/pull/6175))",'**Font-blind ligature substitution removed**: A fixed rewrite turned seven character codes into ligatures regardless of the font in force, though a character code has no meaning without one: two of the codes are fi/fl only in StandardEncoding, two only in MacRomanEncoding, and three in neither anywhere, while in WinAnsiEncoding \u2014 the most common encoding of all \u2014 every one of the seven is an ordinary letter. German text lost each \xdf to an "fl" and each registered sign became "fi" before charset detection ever saw the bytes. Ligatures now come back correctly through the font-aware decoder\'s per-encoding slots ([#6175](https://github.com/rspamd/rspamd/pull/6175))',"**CMap codes carry their byte width**: Mappings were stored under the numeric code alone, so a one-byte `<41>` and a two-byte `<0041>` collided even though a CMap may declare both \u2014 the byte count is what places a code in a codespace, which the input framing already honoured but the tables then ignored, so a one-byte mapping could answer for a two-byte code or be overwritten by one. Both singles and ranges are now keyed by width as well ([#6175](https://github.com/rspamd/rspamd/pull/6175))","**CMaps inheriting from another CMap are refused**: A `usecmap` reference was treated as a marker and thrown away, so a CMap built as overrides on top of a base returned only the overrides \u2014 every code the base was meant to answer for silently vanished, and the surviving ones made the result look like real text. The base is a CMap resource that is not in the file and cannot be resolved, so such a CMap is now refused and the part left undecoded, where the computed-part guard can see it for what it is ([#6175](https://github.com/rspamd/rspamd/pull/6175))",'**Computed text parts kept raw and CED charset names resolved through the alias table**: PDF extraction injects synthetic text parts whose bytes may be valid in no charset at all; running charset re-detection over them produced non-IANA names such as "BIG5-CP950" that ICU rejects. Computed and extracted text parts are now kept raw, so undecodable glyph data stays out of tokenisation and language detection, while CED-detected charsets for regular parts are routed through the alias table (mapping "big5-cp950" to "big5") so legitimate Big5 content still converts ([#6175](https://github.com/rspamd/rspamd/pull/6175))','**EUC-TW misnomer aliases restricted to CED output**: Folding CED\'s invented "CNS" and "EUC" labels into euc-tw in the global substitution table also resolved the charset a message declares \u2014 and a message declaring `charset=euc` is far more likely to carry sloppy EUC-JP or EUC-KR than EUC-TW, so data that used to be left raw decoded cleanly and said something else. Both labels now resolve only where CED output is turned into a charset name, and nowhere else ([#6175](https://github.com/rspamd/rspamd/pull/6175))','**Hyperscan cache usable with a Redis backend**: The TTL-refreshing GETEX is classified by Redis as a write command, but the cache issued it without marking it as one, so with a split master/replica setup every single load failed with "READONLY You can\'t write against a read only replica" and all maps and multipatterns silently ran on the PCRE/ACISM fallback \u2014 easy to miss, because the EXISTS check used to skip recompilation is a genuine read that replicas serve happily, keeping startup fast while no worker ever received a database. GETEX is now used only when reads and writes go to the same servers; otherwise reads hit the replicas and a separate EXPIRE against the master refreshes the TTL. The fix also handles nil replies properly via a published `rspamd_redis.null` sentinel with `lua_redis.is_null()` (previously a missing key crashed inside the error-formatting code), sanitises non-positive TTLs that would have deleted the very database just read, reports why a hot-swap did not happen instead of a misleading file-not-found warning, loads databases asynchronously on worker startup, and compiles regexp maps read after the fork in the process that read them \u2014 previously a large pattern map served over HTTP without a cached copy stayed on PCRE for good, because its compilation queue entry lived in a process that never compiled it ([#6161](https://github.com/rspamd/rspamd/pull/6161))',"**Installed hyperscan databases are not fetched twice**: A map enters the compilation queue via its read callback and is announced by a notification once the database exists, and both paths loaded it, so every map update cost double the traffic \u2014 megabytes for a map of a few thousand patterns. The load is now skipped when the helper already has a database; this cannot keep a stale one, since a helper's patterns never change after creation and every load verifies the content digest","**ARC_ALLOW_TRUSTED scored at most once**: The symbol could be inserted multiple times for a single message, multiplying its score; it is now added at most once ([#6152](https://github.com/rspamd/rspamd/pull/6152))","**History reset clears every row**: Two separate bugs left rows behind. The legacy in-memory ring buffer zeroed only the rows up to the current position and never reset the cursor, so once the buffer had wrapped, the tail kept completed entries that the read path still displayed; all rows are now cleared and the cursor reset. The Redis backend used `LTRIM <key> 0 0`, which retains list index 0 \u2014 the newest entry, since rows are prepended \u2014 so the most recent row always survived a reset; the list is now deleted outright, with a functional test covering the whole flow ([#6185](https://github.com/rspamd/rspamd/pull/6185), [#2910](https://github.com/rspamd/rspamd/issues/2910))","**WebUI searches symbol option values again**: The Tabulator global search built its haystack from symbol name and description only, dropping the option values rendered in the symbols cell \u2014 a 4.1.4 regression that broke searching for values such as `asn:205640`. Options are included in the haystack again, restoring the old FooTable behaviour for both the history and scan tables ([#6169](https://github.com/rspamd/rspamd/pull/6169), [#6168](https://github.com/rspamd/rspamd/issues/6168))",'**Logger format string and argument mismatches**: Eight logger calls across lualib and the Lua plugins supplied a different number of arguments than their format string consumed. Most only produced extra-argument noise, but three misreported or lost information: the clustering plugin\'s Redis success message printed a literal "unknown error", the neural module printed a rule name where a value belonged and dropped the value itself, and lua_stat dropped the actual Redis error from a metadata conversion message ([#6177](https://github.com/rspamd/rspamd/pull/6177), [#6174](https://github.com/rspamd/rspamd/issues/6174))']},{title:"Security",items:["**File, Path and Shm message sources gated by transport**: The `File`/`Path`/`Shm`/`Shm-Offset`/`Shm-Length` headers, their v3 metadata equivalents and the proxy's `File` query argument make rspamd open a path or map a shared memory object that the client names, and they were accepted on any transport \u2014 so any client able to reach a scanner, controller or proxy TCP port could have an arbitrary file readable by the rspamd user parsed and its content reported back. A new per-worker `allow_file_and_shm_inputs` boolean now gates the capability, derived solely from the transport the connection was accepted on: unix sockets always allow these inputs, since access is already controlled by the socket's filesystem permissions, while nothing the client sends takes part in the decision and no other credential \u2014 controller password, enable_password, secure_ip match, encrypted connection \u2014 unlocks it. Gating is applied before the named object is opened, mapped or statted, and two bypasses are closed: query arguments that the proxy turned into trusted upstream headers, and the fuzzy controller endpoints that build their own task. The inputs themselves are hardened \u2014 paths validated before any syscall, files snapshot with bounded reads instead of parsing a shared mapping the caller can truncate mid-parse, shared memory mapped as a page-aligned window rather than the whole object \u2014 the proxy strips client-supplied Shm headers as reserved hop-by-hop headers, the scanner's `max_tasks` now counts accepted body-pending connections, and the controller and proxy gain `max_connections`/`max_connections_per_source` admission limits enforced before any expensive allocation. `encrypted_only` also no longer exempts peers via a locality test that matched IPv6 link-local and site-local addresses, which had let any host on the segment skip the encryption requirement. The option defaults to true in this release with a prominent startup warning per TCP listener, and becomes false in the next major release ([#6162](https://github.com/rspamd/rspamd/pull/6162))","**Controller authentication failures rate-limited per source**: Verifying a controller password runs a deliberately expensive key-derivation function (~25 ms for the catena hash that `rspamadm pw` emits by default) synchronously in the controller's event loop, and the password cache keys on the plaintext that succeeded \u2014 so a stream of distinct wrong passwords fell through to the KDF every time, and roughly 40 attempts per second were enough to saturate a single-process controller and stall every other request it serves. Failed attempts now feed a per-source leaky bucket checked before any password work, so a source over budget gets a 429 in well under a millisecond; a successful login clears the bucket, trusted sources (secure_ip, unix sockets) are never throttled, and throttled requests add no further penalty, so a shared source cannot be pushed into a permanent lockout. Configurable via `max_auth_failures` (default 10, 0 disables) and `auth_failure_window` (default 60 s); note the key is the real TCP peer, so behind a trusted reverse proxy all clients share one bucket. The redundant second KDF run when `password` and `enable_password` are configured to the identical hash is also skipped, halving the cost of every wrong password ([#6162](https://github.com/rspamd/rspamd/pull/6162))"]}],summary:"This release closes a long-standing gap in the scan protocol \u2014 the File, Path and Shm message sources were accepted from any TCP client, letting anyone who could reach a scanner, controller or proxy port have arbitrary readable files parsed \u2014 behind a new transport-derived `allow_file_and_shm_inputs` option, and rate-limits the controller's expensive password verification per source. Fuzzy checks gain a privacy-conscious sender-facts extension over encrypted rules, riding on a wire format made forward compatible so new extension types no longer break older storages, while per-key fuzzy ACLs once again replace the global default as documented. PDF text extraction becomes font-aware, decoding through each font's /Encoding and /ToUnicode CMap instead of guessing over raw character codes, and the hyperscan cache finally works against replicated Redis. Recommended upgrade for all users; operators exposing scanner, controller or proxy ports beyond localhost should review the new `allow_file_and_shm_inputs` and connection-limit options now, ahead of the default flip in the next major release."},{version:"4.1.4",date:"2026-07-29",type:"patch",title:"Patch Release Fixing a Controller Authentication Fail-Open, Regexp Engine Resource Bounds, and jemalloc Startup Segfaults",sections:[{title:"Added",items:["**Read-only access to the Selectors tab**: The `list_extractors`, `list_transforms` and `check_selector` endpoints are now open to read-only users, matching the policy already used for maps \u2014 they only return static catalogs or parse a selector without executing it. `check_message` remains privileged, since it runs a user-supplied selector against a user-supplied message ([#6157](https://github.com/rspamd/rspamd/pull/6157))","**Errors history for read-only users**: The `/errors` endpoint and its WebUI table were gated behind the privileged password. Any authenticated user can now view them: the endpoint returns only Rspamd's internal operational error log (timestamp, pid, level, module, message) \u2014 no message content, PII or secrets \u2014 while read-only users already see strictly more sensitive data in the History tab. Authentication is still required and the endpoint mutates nothing ([#6156](https://github.com/rspamd/rspamd/pull/6156))"]},{title:"Changed",items:["**One shared jemalloc instance per process** (incompatible): The build system searched for `jemalloc_pic` before `jemalloc`, so on Debian it preferred the static `libjemalloc_pic.a` over the shared library and linked it into the common library list used by nine shared objects. Each of them ended up with a complete private allocator holding its own arenas and extent map, so memory obtained through one copy could not be released through another \u2014 and jemalloc-enabled packages segfaulted at startup. CMake now searches for the shared library only, fails the configure step when just a static archive is available (except in fully static builds), and attaches jemalloc per target: `librspamd-server` \u2014 the only code calling `mallctl()` \u2014 plus each executable, pinned with `--no-as-needed` so the linker keeps the otherwise-unreferenced dependency and libc cannot win the `malloc()` lookup. The `malloc_conf` tuning moves into the server library, where it actually takes effect with a shared jemalloc (an executable exports nothing without `-rdynamic`, so it had been silently lost) and now applies to every rspamd binary rather than just the daemon. The jemalloc configuration is also built and tested in CI now, mirroring the shipped Debian/Ubuntu amd64 packages, which had never been covered before ([#6155](https://github.com/rspamd/rspamd/pull/6155), [#6153](https://github.com/rspamd/rspamd/issues/6153))"]},{title:"Fixed",items:["**PCRE2 match data leaked on invalid UTF input**: `rspamd_regexp_search()` allocates the PCRE2 match data before the JIT UTF-8 validation guard, but the early return on invalid input skipped the free at the function tail, leaking one match-data block (sized to the capture count) per rejected input. The guard is entered by any ordinary regexp \u2014 it tests whether a separate raw pattern was compiled, not whether raw matching was actually selected \u2014 and the regexp cache deliberately routes raw matching of UTF-marked classes through PCRE, so a message with a non-UTF-8 text part reached the leak through the normal scan pipeline, once per such regexp per part","**Heap used by a single regexp match bounded**: PCRE2 match contexts set only the backtracking and depth limits, leaving the heap limit at the build-time default of 20 GB \u2014 effectively no limit. The interpreter grows its backtracking frame vector as frame size times depth, so one non-JIT match on a 60 KB subject with a 2 KB frame could allocate around 485 MB before the depth limit intervened, and far more with a larger frame; this path is taken whenever JIT is unavailable or refused for a pattern. An explicit 64 MiB heap limit is now set, preferring `pcre2_set_depth_limit` over the deprecated recursion-limit call where available. The PCRE1 path had no explicit limits at all \u2014 they live in the `pcre_extra` block, and `pcre_study` legitimately returns NULL when there is nothing to optimise \u2014 so a block is now allocated when needed and both match and recursion limits are set, with a much lower depth since PCRE1 backtracks on the C stack. Two PCRE1 drive-bys: study data is freed unconditionally rather than only in JIT builds, and the raw pattern is now studied for the raw matcher instead of handing it study data from a different compiled pattern","**re:matchn() no longer loops forever on empty matches**: An incremental search resumes from the previous match end, so a zero-width match leaves the cursor where it was. `re:search()`, `re:split()` and the regexp cache loop all guard against this, but `re:matchn()` did not \u2014 with unlimited max matches, a pattern that can match the empty string spun forever and hung the worker. This was reachable from configuration: the spamassassin plugin passes unlimited for any `multiple` rule without `maxhits`, so one SA rule with an empty-matchable pattern wedged the process. The same cursor-progress guard is now applied","**Bounded regexp patterns read past their end when generating ids**: `rspamd_regexp_new_len()` parses within the given pointer and length, but id generation then called `strlen()` on the same pointer. Callers passing a slice of a larger buffer \u2014 the composites expression parser and the symcache delayed symbols \u2014 read past the end of the pattern, a heap-buffer-overflow under ASAN whenever the slice is not NUL-terminated. The identity was wrong even when the read stayed in bounds: the same regexp got different ids as a slice and as a standalone string, while two different slices sharing a start address got the same id and collided in the regexp cache, which compares ids only. The length is now passed down to id generation, and three error paths that printed the raw pointer are bounded as well. Ids produced by `rspamd_regexp_new()` and the regexp cache are unchanged","**Regexp data limit propagated to named scopes**: `regexp.max_size` was applied to the default scope only, so regexps registered in a named scope \u2014 multimap `regexp_rules`, for instance \u2014 were matched against unbounded input while the default scope was capped at 1 MiB. Named scope caches are now created inheriting the default scope limit, and setting the limit updates every scope registered so far, which covers both orderings: Lua scopes registered before the regexp module is configured, and plugins registering scopes during filters init","**Lua URL filter consulted at most twice per user field**: Once the user field of a URL exceeded `max_email_user`, the parser consulted the Lua URL filter on every subsequent byte, and each call re-scanned the whole prefix \u2014 control characters, UTF-8 validation, `@` counting, custom filters. The 512-byte user limit in Lua never applied here because the mid-parse fragment contains no `@` yet, so this ran up to the 16 KB threshold: quadratic scanning plus thousands of C-to-Lua transitions, turning a single 16 KB URL into ~0.6 s of CPU. The filter is now consulted at most twice per user field \u2014 once when the limit is first exceeded, and once when the field ends at `:` or `@` so custom filters still see the complete userinfo. Parsing results are unchanged; the same URL now takes about a millisecond","**Words retained for a message bounded by a message-wide budget**: The UTF tokenizer marked decayed words as skipped but still added them to the words vector, unlike the raw tokenizer which drops them \u2014 so the decay bounded nothing. Every word of a text part was retained at 72 bytes each and then normalized and stemmed with several pool allocations per word: a single 5 MB text part of 500k words cost 34 MiB of word vectors plus 27 MiB of pool allocations where the decay is meant to keep around 1200 words. The 200 ms tokenization guard did not help either, since it was only enabled for parts above 1 MiB, so a message of many sub-megabyte parts could accumulate huge word vectors within the 50 MiB message limit. Decayed words are now dropped as the raw tokenizer does; a message-wide budget (100k words, 1 MiB of text) is shared by all text parts and the meta words, with parts that hit it flagged as truncated and the message logged once; the time checks also trigger on the number of produced words; and skipped words are no longer normalized or stemmed. Note that decayed words of UTF parts are no longer visible to word regexp classes and part similarity hashes, which matches the long-standing raw tokenizer behaviour","**CSS tokeniser stack overflow on sequential comments**: `next_token()` called itself after each comment, so an input made of many comments in a row consumed stack proportional to their count. The parser's own recursion limit does not cover this path, and the self-call is only elided when the compiler applies the tail call \u2014 a debug or ASAN build keeps a real call and overflows the stack. The tokenisation loop is now iterative, continuing from the offset past each comment, with doctest coverage for leading, trailing, interleaved, nested and unterminated comments plus a 100k-comment stress case","**actrie startup segfault with jemalloc**: `acism_create()` allocated its buffers with GLib but released them with libc `free()`, which only works while both resolve to the same allocator. With the duplicated per-object jemalloc copies described above, `free()` inside the actrie library ran one jemalloc copy's deallocator on a pointer owned by whichever allocator GLib had bound to, segfaulting inside `rspamd_url_init()` during startup. The whole lifecycle now stays inside GLib, so the pair resolves to one allocator no matter how symbol interposition falls out ([#6153](https://github.com/rspamd/rspamd/issues/6153))",'**rspamadm resolves SRV-based upstreams**: rspamadm never configured the upstreams library, so the upstreams context had neither an event loop nor a resolver. Plain host names still worked, as they resolve synchronously during config parsing, but the SRV form `servers = "service=fuzzy+example.com"` creates a placeholder that is only selectable after asynchronous resolution \u2014 which was never scheduled. Every fuzzy storage configured that way looked empty: `rspamadm fuzzy_ping -l` printed no servers and pinging failed with "no fuzzy storage upstream available". The library is now configured right after the resolver is created, before any command loads its configuration, and since one-shot commands issue their first request before the resolve timer fires, a new `lua_fuzzy.wait_for_storages()` \u2014 used by `fuzzy_ping` and `fuzzy_hash` \u2014 waits for the selected rules to have servers, bounded by the request timeout; an explicit `-s`/`--server` override skips the wait as it bypasses the configured upstreams. A ping that cannot start now also reports the rule name instead of nil']},{title:"Security",items:["**Controller fails closed on a malformed password hash**: `rspamd_is_encrypted_password()` validated only the `$<id>` prefix of a configured password, not that a salt and a key follow it. A hash such as `$1$` therefore passed the check, reached the verification with both components absent, skipped the comparison block entirely and returned its initial value of TRUE \u2014 any supplied password authenticated, and was then cached as the valid one. The result is now initialised to FALSE and set only after a completed KDF comparison, and the malformed hash is logged instead of silently accepted. The component extraction also indexed three bytes into the string without checking its length, reading past the terminator for short inputs and making the fail-open depend on adjacent heap bytes; it now takes the length and bails out when a component is missing. The startup sanity check re-ran the same prefix-only test, so a malformed password also passed configtest silently \u2014 it now decodes the salt and the key and validates their lengths against the pbkdf matching the hash id, which additionally fixes a hardcoded reference to the first pbkdf in the list"]}],summary:"This release closes a critical controller authentication fail-open \u2014 a malformed password hash such as `$1$` accepted any password, and configtest did not catch it \u2014 and fixes the startup segfault in jemalloc-enabled packages by linking one shared allocator instance per process instead of a private copy per shared object, a configuration that is now also exercised in CI. The regexp engine gets a broad hardening pass: an explicit heap bound for a single match on both PCRE2 and PCRE1, a match-data leak on invalid UTF input reachable from the normal scan pipeline, an infinite loop in `re:matchn()` on empty-matchable patterns, an out-of-bounds read when generating ids for bounded patterns, and the data limit finally applied to named scopes. Rounding it out: quadratic URL parsing with per-byte Lua filter calls is gone, tokenizer word retention is bounded by a message-wide budget, the CSS tokeniser no longer overflows the stack on comment floods, and rspamadm handles SRV-based fuzzy upstreams. Recommended upgrade for all users, and strongly recommended for anyone running the controller with worker passwords."},{version:"4.1.3",date:"2026-07-25",type:"patch",title:"Hardening Release with DKIM/SPF Correctness Fixes, Parser Resource Bounds, Fuzzy Hash Observability, and a jQuery-Free WebUI",sections:[{title:"Added",items:["**Structured fuzzy match results and a diagnostics API**: Every fuzzy match now produces a structured record \u2014 rule, symbol, upstream, stored and queried digests, match type, probability, score, flag, value and hash timestamp \u2014 kept in the `fuzzy_matches` mempool variable and exposed to Lua via `task:get_fuzzy_results()`. All queried hashes, misses included, are recorded in `fuzzy_checked`. The symbol option for non-exact matches now carries both hash prefixes (`flag:found:prob:type:queried`), so a match is diagnosable from a scan report alone. The storage sets a flag bit in the first reserved byte of the reply when the digest field holds the digest actually resolved by the backend, and the client propagates it as a `confirmed` field, removing the guesswork about echoed queries when talking to legacy storages. A new `fuzzy_check.check(task, cb, rule, timeout[, hashes][, server])` Lua API queries a storage either with the hashes generated from the task message (shingles included) or with an explicit list of hex digests, reporting per-reply results including misses and error codes","**Configurable fuzzy probability weight curve**: The score multiplier for non-exact matches used to be `sqrt(prob)`, a concave curve anchored at zero. Since the storage never returns a probability below the shingle match threshold of 0.5, the whole reachable range collapsed into roughly (0.73 .. 1.0], so a marginal 17/32 shingle overlap scored almost as high as an exact match \u2014 combined with high-weight deny lists this turned weak matches into instant rejects. The curve is now `((prob - prob_bias) / (1 - prob_bias)) ^ prob_power`, anchored at the match threshold (`prob_bias` 0.5). With the default `prob_power` of 1.0 the multiplier is simply the fraction of the way from the threshold to an exact match (19/32 \u2192 0.19, 24/32 \u2192 0.50, 28/32 \u2192 0.75); raise it per rule for harsher discounting of weak matches. The old zero-anchored curve is not preserved. Applies to text and HTML hashes; small images keep their existing normalized curve","**Persistent shingle sets in the redis fuzzy backend**: Shingle key suffixes are now stored as the `S` field of the digest hash on every ADD, making each digest self-describing and closing two long-standing blind spots. Digest-only deletions (for example periodic deletions driven by a hash list) previously left the digest's shingle slots orphaned; they are now reconstructed from the persisted set and removed when still owned by the deleted digest. REFRESH used to touch the shingle slots of the message that happened to trigger it \u2014 a different message by definition \u2014 and now refreshes the slots the digest still owns and reclaims the vacant ones, so a hash that keeps matching keeps its full shingle anchor instead of decaying. The sqlite backend needs no changes, since its shingles table already cascades on delete","**Per-hash storage introspection**: `rspamadm control fuzzyhash <128 hex digest>` asks all fuzzy workers for storage-side diagnostics of a specific hash: flag slots and values, creation time, remaining TTL and \u2014 using the newly persisted shingle set \u2014 slot ownership (owned/foreign/vacant out of total). Ownership directly answers the central false-positive triage question: can this hash still produce fuzzy matches, or has its shingle anchor decayed? The redis backend gathers everything in a single server-side script round trip; sqlite queries the digests and shingles tables synchronously","**Unkeyed client statistics**: Traffic from clients without a key (for instance those permitted by `allow_update`) was invisible in the key statistics, which made storage writers untraceable. Such clients are now tracked under a dedicated `unkeyed` pseudo-key with the same aggregate and per-IP counters as regular keys","**rspamadm fuzzy_hash command**: Computes the fuzzy hashes of a message per configured rule, matching the scanner tokenization bit for bit, optionally checks them against the storage with shingles included (`-C`), and queries storages for explicit hex digests (`-H`) \u2014 printing found and queried digests, probability, flag, timestamp and whether the storage confirmed the matched digest","**ClickHouse fuzzy match export**: New `Fuzzy.Hash`/`Fuzzy.Queried`/`Fuzzy.Rule`/`Fuzzy.Prob`/`Fuzzy.Flag` nested columns (schema version 11) filled from the structured fuzzy results, so false-positive investigations can start from the scanner-side journal instead of storage forensics","**Annotated X-Rspamd-Fuzzy header**: `milter_headers` now builds the header from the structured fuzzy results, annotating every matched hash with the rule, flag, probability, the queried hash for non-exact matches and the storage timestamp. It falls back to the legacy `fuzzy_hashes` pool variable when no structured results exist","**lua_http forbid_local**: `rspamd_http.request()` used to connect to whatever the URL or its DNS resolution yielded, loopback, link-local and RFC1918 destinations included. A new `forbid_local` option is enforced at the single connection chokepoint \u2014 after keepalive, upstream, numeric and DNS address selection \u2014 using the address class check plus the configurable `local_addrs` radix. It is enabled by default in `url_redirector`, where message URLs and their redirect targets are attacker-controlled and hop limits alone do not stop a crafted redirect from probing cloud metadata endpoints or internal services; operators resolving internal redirectors can set `forbid_local = false`","**ARF report enrichment**: Microsoft's JMRP feedback loop ships a well-formed feedback report whose `message/feedback-report` block only carries Feedback-Type, User-Agent and Version \u2014 Source-IP, Arrival-Date, Original-Mail-From and Original-Rcpt-To live only in the embedded original message. `lua_feedback_parsers` now recovers those fields from the original headers, filling only what the report itself left empty and recording provenance in `result.derived` so callers can tell reported data from inferred data: source IP from X-Originating-IP, then `client-ip=` of Received-SPF or Authentication-Results, then the first public IP while walking the Received chain; arrival date from the topmost Received timestamp, else Date; original mail from from Return-Path, else `smtp.mailfrom`; original recipient from Delivered-To, X-Delivered-To or envelope-to; and reported domain falling back to the envelope-from domain. `Received-Date` is also accepted as an Arrival-Date alias in the report block"]},{title:"Changed",items:["**WebUI drops jQuery entirely** (incompatible): Every WebUI module \u2014 the AJAX layer, common helpers, the leaf modules (graph, selectors, history, symbols), config, libft, stats, upload and the main module \u2014 is migrated to native DOM and `XMLHttpRequest`, and the vendored jQuery build is deleted. The rewritten `query`/`queryServer` preserve jQuery's semantics: neighbours fan-out and aggregation, per-server error dispatch, `statusCode`, timeout, GET data appended as a query string, the default Content-Type for POST bodies, NProgress download progress and jqXHR-compatible callback arguments. Bootstrap 5 moves from the jQuery bridge to the vanilla constructor API, the `jquery.stickytabs` plugin becomes an inline implementation using the BS5 Tab API, and D3Evolution and D3Pie are updated to versions that dropped their own jQuery dependency. Drive-by: the `modalDialog` backdrop attribute is renamed to its Bootstrap 5 name `data-bs-backdrop`, so the static-backdrop behaviour is actually applied ([#6124](https://github.com/rspamd/rspamd/pull/6124))",'**Font Awesome replaced by a local SVG sprite** (incompatible): The "SVG with JS" framework \u2014 roughly 900 KB of JavaScript plus its CSS \u2014 is removed in favour of a generated 35-glyph sprite. An initial sweep plus a MutationObserver replaces `<i class="fas fa-name">` with an inline `<svg><use href="img/icons.svg#fa-name"/></svg>`, carrying over non-class attributes and modifier classes such as `fa-spin`. Each icon keeps its natural aspect ratio, and CI verifies that every icon referenced in the markup is listed in the manifest and that the generated sprite is not stale ([#6145](https://github.com/rspamd/rspamd/pull/6145))',"**HAVE_ED25519 scoped to OpenSSL key parsing** (incompatible): All Ed25519 crypto in the DKIM path is libsodium, which is a mandatory dependency \u2014 verification, signing, key sizes and the public/private match never touch OpenSSL. `HAVE_ED25519` nevertheless probed OpenSSL for `EVP_PKEY_ED25519` and gated that libsodium code, so an OpenSSL build without Ed25519 refused to verify ed25519 signatures even though libsodium was fully capable of it. The probe is kept at the single site that genuinely needs it \u2014 unwrapping a PEM/DER private key for signing \u2014 and dropped from the nine libsodium-only sites"]},{title:"Fixed",items:["**DKIM public key size and modulus width**: `rspamd_dkim_make_key()` took the `p=` tag of a DNS TXT record, checked only that it held at least three bytes, then allocated two buffers of that size and handed the decoded result to OpenSSL. Since rdns falls back to TCP on a truncated reply, a zone can serve roughly 64k of key material per selector, and the LRU key cache is capped by entry count rather than by bytes \u2014 so a hostile zone could pin hundreds of megabytes with one message per selector. `p=` is now capped at 4096 base64 characters before any allocation (a 4096-bit RSA key needs 736), and keys wider than 8192 bits are rejected once parsed, since verification cost grows with the modulus and RFC 8301 only requires verifiers to handle up to 4096 bits. Only verification is affected; signing keys take a different path","**DKIM empty-line skipping read before the body slice**: The backwards scan for trailing empty lines guarded its lookbehind, but the else arm of that guard then read one byte before the start of the slice anyway \u2014 reachable from any body consisting only of line endings. The byte in question is the last byte of the header/body separator, so it stayed inside the message allocation, but the canonicalised result depended on data outside the input. The probe is replaced by assigning the empty-body sentinel the function already uses; a differential run over every body up to four bytes drawn from CR, LF, space, tab and a letter gives identical results for both canonicalisation types across all 1560 cases, so no body hash changes",'**DKIM max_sigs applied before work, and every signature counted**: The per-message signature limit was tested at the bottom of the loop body, after the DNS request for the current signature had already been started, and it incremented before comparing with `>`, so the default of five permitted six. Worse, the three `continue` paths above it \u2014 parse failure, `trusted_only` skip and a failed key lookup \u2014 jumped over the increment, so signatures taking those paths never counted at all and the real bound was `max_mime_headers` (100000). The check now runs at the top of the loop, before any work is done for the header, and counts every signature header examined. This shifts the meaning of `max_sigs` from "signatures whose key we looked up" to "signature headers examined": five junk signatures ahead of a valid one now push the valid one out, which was already reachable with cheap parseable decoys',"**DKIM h= header list bounded**: `rspamd_dkim_parse_hdrlist_common()` counted every token in `h=`, preallocated that many pointers and then allocated a name string and a header record per non-empty token, so a size-limited message expanded into roughly 32 bytes of allocation per two input bytes. The two loops used different predicates, so an `h=` made only of colons reached the full preallocation while creating no items \u2014 the cheapest form of the amplification. The item count also drove the header selection loop, where each entry costs a hash lookup plus a duplicate walk. The list is now capped at 1000 items, checked before any allocation; the shipped `default_sign_headers` list has 27 entries. Both parsing directions share this function, so signing is bounded too","**SPF returns permerror when a DNS limit is hit**: Exceeding `max_dns_requests` or `max_dns_nesting` only left the offending element unparsed, so the record was still evaluated with whatever fitted in the budget and usually ended up as a definitive fail via its trailing `-all`. RFC 7208 \xa74.6.4 requires permerror instead, and rightly so: the record has not been evaluated to the end, hence its verdict is unknown rather than negative. Records carrying any flag are not put into the LRU cache, so a permerror is not stored for the TTL of the record","**SPF include/redirect nesting limit enforced**: `rec->nested` was checked but never incremented, so `max_dns_nesting` had no effect at all and an include chain was bounded only by the DNS request counter \u2014 which was itself compared with `>` while being incremented after the check, so 31 DNS elements were evaluated with a limit of 30. The depth is now stored per resolved element (resolution is asynchronous, so there is no call stack reflecting position in the tree) and checked when an include or redirect is about to be followed, and the request counter uses `>=` so exactly `max_dns_requests` elements are evaluated. Flattening a record recurses over the same chain and is bounded by a hard nesting limit that applies even when the configurable limits are relaxed or disabled","**SPF address lookups from mx/ptr expansion bounded**: Names returned by mx and ptr replies were expanded to A/AAAA requests while checking a DNS counter that never changed, and SPF uses the forced resolver API that bypasses `dns_max_requests`, so a single large RRset produced an unbounded number of queries. Both RFC 7208 \xa74.6.4 limits are now enforced \u2014 10 address lookups per mx/ptr element \u2014 alongside a new per-record budget for all expansions, configurable as `max_dns_expansions` (100 by default). Exceeding them for mx, or running out of the record-wide budget, yields permerror for the whole record instead of silently dropping the element (which used to surface as `R_SPF_FAIL` via `-all`); extra ptr names are ignored as the RFC requires","**SPF exists attached to the wrong element**: `parse_spf_exists` picked the current record's last resolved element, which only holds while nothing else has appended an element of its own \u2014 an `include` earlier in the same record does exactly that, so from then on every `exists` in that record was attached to the include's element. The element is now passed down as the other mechanisms already do. Since the domain spec of an `exists` is expanded before this point, the visible effect so far was limited to a wrong domain in the diagnostics of an unresolvable `exists`","**Fuzzy admission checked before parsing UDP commands**: Every datagram used to be parsed, and decrypted when encrypted, before any admission check ran \u2014 the blocklist and the rate limit were only consulted well after a session allocation, an ECDH, a MAC verification and the Lua pre-handlers. The blocklist is now checked in the UDP read loop before allocating a session, so a blocked peer costs one radix lookup; a failed key lookup or MAC check is logged at debug rather than error level, since both are reachable by anyone who can send a datagram and an error line per packet turned a spoofed-source flood into a log volume attack. The per-source rate limit is extended from FUZZY_CHECK to PING and STAT, which are answered unauthenticated and were previously unmetered, and rate-limited PING/STAT are dropped rather than answered. The ratelimit bucket allocation is also skipped when rate or burst are unset, which previously allocated an LRU entry per masked source that could never limit anything. `blocked_requests`, `decrypt_errors` and `ratelimited_requests` are reported in fuzzystat so the drops that are now silent stay observable","**Fuzzy sqlite backend id stack overread**: The backend id was built with `%xs`, which treats its argument as a NUL-terminated string and calls `strlen` on it \u2014 but the hash is a raw 64-byte digest with no terminator, so building the id read past the end of the stack buffer on every sqlite backend open. An explicit length is now passed","**Fuzzy error replies name the rule and the server**: 403 (rate limit), 503 (access denied) and 415 (encryption required) replies used to insert their symbols silently, so with several rules configured there was no way to tell from the logs which rule and which server produced the error. Both the UDP and TCP reply paths now log the rule name and the upstream","**Nested comment depth in the ragel header parsers**: The Content-Disposition and SMTP date grammars parse RFC 5322 nested comments via `fcall`, which pushes an entry onto a heap-allocated state stack per nesting level. The stack had no depth limit, so a single header allocated memory proportional to its own length \u2014 roughly 12 bytes per input byte, with unbalanced opening parens as the worst case \u2014 and bounded only by `max_message`, a crafted header could push a worker to around 530 Mb of transient allocation, where a realloc failure hit an assertion and aborted the process. Nesting is now capped at 8 levels and the parser drops into the error state once it is exceeded, so it stops after a fixed number of bytes rather than consuming the whole header; realloc failure now fails the parse and keeps the old allocation","**MIME parser, MIME headers and archive metadata resource bounds**: All three now bound the resources a single message can make them allocate","**Content-Type parameters per header, and RFC 2231 ordering**: A single Content-Type or Content-Disposition field can carry an unbounded number of parameters, each materialising a pool object plus a hash or list entry, so a message bounded at `max_message` could amplify into millions of allocations from one folded header. The existing header-count cap limits header fields, not parameters within a field. Parameters are now capped at 1024 per header, counted rather than deduplicated so a same-name flood is bounded too; Content-Type flags the truncation as broken. Also fixed: `rspamd_cmp_pieces` subtracted two unsigned RFC 2231 continuation ids and truncated to `int32_t`, reversing the reconstruction order when the ids differ by more than `INT32_MAX`","**Per-part newline metadata bounded, budget made task-global**: Newline normalization recorded every newline as a pointer array entry plus a mempool exception object and a GList node, then sorted the whole exceptions list, so a newline-dense body turned a few MiB of input into hundreds of MiB of heap. Recorded newline positions are capped at 100k per part \u2014 text normalization and line counters are unaffected \u2014 with a part flag on truncation exposed as `newlines_truncated` in `textpart:get_stats()`, and the budget is now task-global rather than per-part","**Plain text parts linked to their HTML alternative**: The alternative part search compared the raw `IS_TEXT_PART_HTML` flag value (0 or 4) against the boolean `want_html` argument (0 or 1), so a text/plain part never matched its text/html sibling and only the html-to-plain direction of `alt_text_part` was ever populated","**Alt-part linking and fasttext langdet cost bounded**: Alternative text part linking used one subtree scan per text part, so a crafted multipart/alternative with thousands of sibling text parts made linking quadratic \u2014 around 100M sibling visits at the MIME part cap. All searches of a task now share a single visit budget, and the remaining parts are left unlinked with a warning once it is exhausted. Fasttext language detection fed up to 1M words into the model with no bound on word length or resulting token count; since the UTF tokenizer intentionally does not enforce `max_word_len`, a body made of huge unspaced words expanded into three or four subword ngram ids per character. Overlong words are now skipped, the total token count is capped, and the shim refuses OOV subword expansion for words longer than any real dictionary entry","**HTML DOM recursion eliminated**: `html_append_tag_content`, `traverse_block_tags` and `html_debug_structure` are converted from native-stack recursion to explicit heap stacks, and `html_tag::depth` is maintained incrementally instead of walking the parent chain per tag. With `max_tags` at 8192 a message can legally nest around 8190 elements, which overflowed 512KB worker-thread stacks; a regression test parses 8190 nested divs","**HTML attributes bounded per tag and per task**, and **synthetic tags capped** with a fix to the tag balance loop","**HTML image style dimension parsing**: The parser found a digit but called `rspamd_strtoul` from the start of the substring, ignored its return value and assigned the result regardless. For substrings longer than 22 characters `rspamd_strtoul` returns before writing the value, leaving image height and width set from an uninitialized variable; shorter CSS parsed to zero because the substring began with a colon or whitespace. The digit run is now parsed from the located digit with a bounded length, and assigned only on success","**Quadratic Content-ID image linking**: Linking images to their parts no longer scales quadratically with the number of parts","**7zip folder count bounded and parser progress guaranteed**: The folder-count limit was applied only in the internal-folders branch, so the external branch preserved an unchecked 64-bit count; the later `kCodersUnPackSize` loop's error branch then logged once per folder without consuming input, allowing a non-progressing CPU and log loop that the synchronous parser cannot be interrupted from by the task timeout. The limit now applies immediately after reading the folder count, external-folder metadata is rejected rather than accepted with inconsistent downstream state, the error branch returns early so every loop iteration advances or bails out, and the folder tables move from stack allocation to the task mempool","**max_urls enforced at the central insertion boundary**: All URL sources \u2014 HTML links, query URLs, images, displayed URLs, subject URLs and Lua-injected URLs \u2014 now respect the configured `max_urls` limit rather than just plain-text callbacks, and the text pre-checks are exact rather than off by one. A duplicate part-url add for newly-seen text URLs is also removed","**url_suspect dead branch and mailto URLs**: Both arms of the `length_thresholds.suspicious` branch inserted the very same `URL_USER_PASSWORD` finding, so the threshold never affected anything; the branch and its now-unused setting are removed. mailto URLs are also rejected explicitly in the same check, since every email address carries a local part and the presence of a user is meaningless there","**Task zstd decompression bounded by max_message**: The legacy compressed-message path allocated the output buffer straight from the frame-advertised decompressed size and doubled it without any ceiling, so a zstd bomb within the HTTP size limit could balloon memory far beyond `max_message`; the decompression-error return also leaked the output buffer, which was registered with the task pool only on success. Frames advertising more than `max_message` are now rejected upfront, the growth loop enforces the limit, and the buffer is freed on all error returns. The same upfront clamp is applied to the v3 multipart path, which now also honours `max_message == 0` as unlimited, matching the HTTP layer convention","**One bounded zstd decompression helper across HTTP, proxy and maps**: The streaming decompression loop was copy-pasted in seven places with inconsistent bounding. The proxy had no output ceiling at all \u2014 on request and response bodies and on the v3 multipart result and body parts, a zstd bomb from a client or backend could balloon memory without limit. The task and v3 protocol paths were bounded but enforced the limit only when the buffer filled, letting output overshoot `max_message` up to twofold, and never validated the final length; the map shm-cache, file-cache and static paths grew their buffers with no ceiling. A single `rspamd_zstd_decompress_bounded` now validates the frame-advertised size before any allocation, caps buffer growth strictly at the limit, drains pending decoder output and frees the buffer on every error path. All seven sites are converted: `max_message` bounds the task, protocol v3 and proxy paths, `max_map_size` the map cache and static paths","**HTTP request bodies bounded on the controller, proxy and control sockets**: The normal worker caps HTTP bodies at `max_message`, but the controller router, the proxy client connection and the main control socket never set a maximum \u2014 a declared Content-Length caused an unbounded preallocation and chunked input could grow until the connection ended, all before routing or authentication. The HTTP router gains a `max_size` knob applied to every accepted connection, with `max_message` set as the cap on all three; oversized requests are rejected with 413 on the controller and a closed connection on the proxy","**HTTP read deadline enforced when data is pending at timer expiry**: The read timer is a one-shot timer. When it expired while data was already waiting in the socket, the handler drained and parsed that data, and if the message remained incomplete the connection continued with only the I/O watcher active and no deadline at all. A 408 is now reported instead, unless the drained bytes completed the message","**Remote HTTP map sizes bounded, compressed and decompressed**: The map client connections never set a maximum body size, so a map server could feed an arbitrarily large body that is fully retained in shared memory, and the zstd path allocated the output buffer from the frame-advertised size and then doubled it without limit. A new `max_map_size` option (256 Mb by default, 0 disables) is applied as the HTTP body limit on both map client connections, frames advertising a larger decompressed size are rejected upfront, and the same ceiling is enforced in the streaming growth loop. It can be overridden per map with `max_size` in the `maps { }` block, like the existing timeout and keepalive knobs",'**Map signature file mapped as a file**: `rspamd_map_check_file_sig()` receives a filesystem path and maps the `.pub` counterpart as a file, but reached for the `.sig` one via the shared memory helper \u2014 which on any platform with POSIX shared memory means `shm_open("/path/to/map.sig")`, rejected outright by the flat shmem namespace, so verification of a signed file-backed map could never succeed there',"**Credentials redacted from map error logs**: Map URIs may carry HTTP basic-auth userinfo, which was logged verbatim in critical messages that feed the `/errors` ring buffer exposed via the controller, leaking credentials to anyone reading it. Error logs now use a redacted copy with the userinfo replaced; the real URI is kept intact, since it serves as the configuration key and carries the userinfo needed for the Authorization header ([#6146](https://github.com/rspamd/rspamd/pull/6146))","**Shared memory mapping helpers hardened**: `rspamd_shmem_xmap()` mapped whatever the name resolved to, so a fifo, a device or a directory ended up in `mmap(2)` with a meaningless size, and the size output was left untouched on failure while its file counterpart has always reported a defined value. Non-regular and empty segments are now rejected explicitly, the 32-bit address space overflow is guarded, the failing errno is kept intact across `close(2)`, and the size output has the same meaning as in the file variant. `rspamd_shmem_mkstemp()` also retried `shm_open(2)` forever on EEXIST, so the loop is now bounded and a permanently colliding namespace cannot spin a worker","**lua_http responses bounded by default**: Lua HTTP client responses were unlimited unless a plugin explicitly passed `max_size`, and a negative `max_size` was converted straight to an unsigned type, silently producing an effectively unlimited cap. A new `max_lua_http_response` option (256 Mb by default, 0 disables) is applied whenever a request does not specify `max_size`; an explicit `max_size` of 0 still disables the limit per request, and negative values are rejected with a logged error instead of wrapping","**lua_http errors delivered to coroutine callers**: DNS-stage failures \u2014 resolve error, no records, connection refused \u2014 went through a path that unconditionally invoked the callback reference, but coroutine-style requests have no callback, so the error was swallowed and the yielded thread never resumed. The thread is now resumed with `(err, nil)`, mirroring the connection error path. Synchronous failures (unparseable URL, blocked session, immediate connection or DNS-send failure) returned a bare `false`, which coroutine callers read as `err = false, response = nil` \u2014 a crash on `response.code` for every caller checking `if not err`; those now return `(err, nil)` as well","**lua_tcp double free on connection release**: A Lua callback calling `close()` sets a sticky finished flag observed by more than one release site, dropping the connection ownership reference twice and freeing the connection data while the handler was still running. The ownership drop is now guarded by a one-shot flag so the first site to observe termination wins and the rest become no-ops",'**PDF text line breaks**: The Td/TD text positioning operators never produced a newline \u2014 the grammar did not capture the operator name, so the handler compared a numeric operand against the string `Td` (never true) and additionally read the wrong operand. Consecutive text lines were therefore concatenated, so for example "\u2026June 1, 2026." followed by "You may obtain\u2026" became "2026.You", which the URL parser mis-detected as `http://2026.you` and raised false positives. The operator is now captured and a newline rendered when the vertical displacement is non-zero',"**Duplicate module sections warned about**: `rspamd_config_get_module_opt()` returns the rule or option object from the first section only when a module section is duplicated at the top level \u2014 for example by a stray file in `modules.d` \u2014 so the remaining sections are silently ignored and their rules are never loaded. An explicit warning is now emitted per configured C module, so configtest and the startup log surface the problem instead of silently dropping rules. Lua modules are unaffected, since they flatten the whole duplicate chain",'**WebUI: malformed /stat response routes to the login dialog**: The connect probe swallowed JSON parse errors, so a 2xx `/stat` with a non-JSON body \u2014 truncated by a proxy, a captive-portal page, corruption \u2014 loaded an empty, half-loaded main UI with a "Cannot get server status" alert and no login path. The body is now parsed explicitly and any failure shows the connect dialog, matching the pre-migration behaviour. The unreachable 304 success branch inherited from jQuery\'s default success range is also removed, since the controller sends `Cache-Control: no-store` and never emits Last-Modified or ETag for these endpoints',"**WebUI: smooth scroll restored on the Scan tab**: The jQuery removal replaced the animated scroll with `window.scrollTo({behavior:\"smooth\"})`, which honours `prefers-reduced-motion` and is interruptible by Tabulator's reflows, so the scroll frequently did not happen. A requestAnimationFrame-based implementation (400 ms ease-in-out, matching jQuery's default) is used for both Scan scrolls, preserving the previous behaviour",'**WebUI: refresh spinner no longer restarts mid fan-out**: The active-request counter was decremented at the top of the completion handler, before the success callback, so during an "All SERVERS" query the count dipped to zero between the neighbours probe and the per-neighbour requests, re-firing the start hook and restarting the spinner\'s CSS animation. The decrement moves to the end, after the success and complete callbacks',"**WebUI: jQuery-removal regressions**: The optional `fuzzy_hashes` key is guarded in the fuzzy storage stats, which threw on every Status refresh when `fuzzy_check` was disabled or a neighbour was down; the hide and show slide animations are cancelled once the final state is committed, so a pinned height no longer makes a later re-show animate 0\u21920; a single element exposing a numeric `length` (such as `<select>` or `<form>`) is treated as one node rather than a collection; a null neighbours body is tolerated; the active-tab lookup is null-guarded in two spots; and a dead upload parameter is dropped"]},{title:"Security",items:["**DKIM bh= length not bounded before the body hash comparison (out-of-bounds read)**: The bh= length validation covered RSA and ECDSA but omitted ed25519, so an ed25519 signature carried a fully attacker-controlled body hash length \u2014 `parse_bodyhash()` sizes the buffer from the base64 input and stores whatever length decoding yields. That length was then passed to `memcmp()` against buffers that are always `EVP_MAX_MD_SIZE`, reading out of bounds once bh= decodes to more than 64 bytes. The comparison runs before the key type and signature algorithm compatibility checks, so a mismatched key record does not prevent it; an attacker only needs a parseable DKIM key at a domain and selector under their control, and since the overread scales with the header size it can walk off the stack rather than merely tripping a sanitizer. Ed25519 is added to the parse-time length check, and the comparison site is additionally guarded by an algorithm-agnostic length equality test so a future algorithm cannot reopen the same hole by missing a parse-time case","**Fuzzy TCP session use-after-free on aborted connections**: Every terminal condition on a fuzzy TCP connection \u2014 EOF, read error, invalid frame length and the timeout \u2014 simply released the session, which is not enough: each in-flight command retains the session too, so with commands outstanding the refcount stayed nonzero, the destructor did not run and both watchers remained armed. EOF is permanently readable, so the next poll re-entered the I/O handler and released the owner reference a second time, freeing the session while the command callbacks still pointed at it; the timeout had the same defect, being a repeating timer. A single guarded termination path now stops the watchers, closes and poisons the descriptor and drops the owner reference exactly once, and late replies for a closed session are discarded instead of resurrecting I/O on a dead connection. A fatal write error, previously conflated with a retryable short write, left the write watcher armed on a permanently writable error condition and spun forever; such a session is now closed. Reproduced under ASAN by aborting a connection while its backend lookup was still pending",'**Shared memory segment bounds not validated (protocol)**: `Shm-Offset` and `Shm-Length` were each checked against the segment size on their own but never together, so an offset near the end combined with a full length passed both checks and produced a body running past the mapping \u2014 SIGBUS on a page-multiple segment, adjacent memory in the scan result otherwise. Both protocol loaders carried their own copy of the code and hence of the bug. On platforms without POSIX shared memory the segment name is an ordinary path, and `open(2)` on a name that happens to be a fifo blocked the whole worker until somebody opened the writing end. Both versions now share one validated helper that opens non-blocking and rejects empty, over-long and control-character names, non-numeric or overflowing offsets and lengths, non-regular and empty segments, offset plus length beyond the segment, and payloads over `max_message`. An offset without a length now means "up to the end of the segment" rather than "the whole segment", and the v3 metadata path rejects negative values and no longer leaks an fstring plus a token per request',"**Shared HTTP body storage lifecycle (double free, descriptor leak, closing an unrelated socket)**: The proxy cleared the shared-memory flag by hand before installing a new body, so the following `set_body()` cleaned up the storage as if it were an fstring and freed the refcounted shared storage the union aliases, while the segment descriptor and its mapping leaked. A new helper releases the shared storage while the flags still describe it. The storage cleanup also skipped a segment sitting on descriptor 0, leaking both the descriptor and the mapping whenever the kernel handed us that one \u2014 and releasing it uncovered why that looked harmless: a message can carry the shared flag before a segment exists for it, since a body-less copy inherits the flag from its origin, and the zero then read as a valid descriptor and closed whatever occupied slot 0, which in a proxy worker is the accepted client socket. The descriptor is now initialised to -1 so it is only ever valid for a real segment. Also: the name is reset after release so the next cleanup cannot release it twice, an `fstat(2)` hidden inside an assertion is dropped, non-regular and empty descriptors are rejected, the length overflow in body growth is guarded, and a copied message's body is validated to fit the segment it is mapped from"]}],summary:"This release is dominated by hardening: a critical out-of-bounds read in DKIM body hash comparison reachable with an ed25519 signature, a use-after-free on aborted fuzzy TCP connections, unvalidated shared memory segment bounds in the protocol, and a shared HTTP body storage lifecycle bug that could close an unrelated socket in a proxy worker \u2014 alongside a broad sweep of resource bounds across the MIME, HTML, archive, URL, zstd, HTTP and map paths that previously let a size-limited message amplify into hundreds of megabytes of allocation. SPF and DKIM also gain real correctness fixes: nesting and DNS limits that were checked but never enforced, permerror where a fail used to be reported, and ed25519 verification that now works on OpenSSL builds lacking it. On the feature side, fuzzy hashing becomes genuinely observable \u2014 structured match results in Lua, ClickHouse and the milter header, per-hash storage introspection, persistent shingle sets and a rebalanced probability weight curve \u2014 and the WebUI completes its migration off jQuery and Font Awesome. Recommended upgrade for all users."},{version:"4.1.2",date:"2026-07-21",type:"patch",title:"Patch Release with a Critical MIME Recursion DoS Fix, Sender-Identity Preservation for DMARC/SPF, Composite Lua Conditions, and the WebUI Tabulator Migration",sections:[{title:"Added",items:["**Composite conditions and explicit dependencies**: Composite atoms can now be gated with synchronous Lua functions, by analogy with the `re_conditions` mechanism of mime expressions: `rspamd_config:add_composite()` accepts a full definition table with a `conditions` map from symbol name to `function(task, symbol)`, where `symbol` arrives in the `task:get_symbol()` layout. A condition may return true/false or a number used as the atom weight; it is ANDed with the atom's option filters, and a failed condition is treated exactly like a missing symbol, so removal policies are not applied. Since a condition can consult symbols invisible to the expression itself, an optional `depends_on` list feeds the first/second composite pass placement as if those symbols were expression atoms \u2014 letting a condition safely consult, for example, a postfilter symbol ([#6126](https://github.com/rspamd/rspamd/pull/6126))",'**Settings apply policy and strict explicit_enable gating**: Symbols carrying the `explicit_enable` flag now genuinely require explicit enabling. Previously they were executed whenever *any* settings were applied to a task, regardless of content; now they run only when listed in `symbols_enabled` of the applied settings or force-enabled via `task:enable_symbol()`. To combine the default-allow behaviour with selectively unlocked symbols, the `apply` block gains a policy option: with `policy = "implicit_allow"`, `symbols_enabled` no longer switches the settings into whitelist mode \u2014 all symbols stay enabled, the listed ones are merely unlocked, and `symbols_disabled` still subtracts. The policy is honoured for raw settings passed via the `Settings` header as well, and symbols listed in `symbols_enabled` are now always tracked as force-enabled, which previously missed the map-driven settings path ([#6144](https://github.com/rspamd/rspamd/pull/6144))',"**static_embed neural provider**: A Model2Vec-style static token-embedding provider, the cheap multilingual successor to `fasttext_embed`. Words from rspamd's regular tokenization pipeline are re-tokenized into WordPiece subword tokens (ICU-based Bert normalization, Bert pre-tokenizer, greedy WordPiece) and embedded by mean-pooling rows of a precomputed float32 matrix, mmap-ed and shared between workers \u2014 no neural forward pass, no FFI, and no new dependencies. Model loading is strict and fail-fast: unsupported normalizer/pre-tokenizer/model types, non-mean pooling, a non-float32 matrix, or a vocab/matrix size mismatch fail the load instead of degrading silently. The subword tokenizer stays internal to the vectorizer, so Bayes tokens and fuzzy hashes are untouched, and a `get_token_vectors()` method exposes the per-token embedding sequence for offline consumers such as external trainers ([#6118](https://github.com/rspamd/rspamd/pull/6118))","**Neural forced-learn fast path and training freeze**: Two new training controls. `train.forced_learn_minimal_scan` (default on for symbol-independent rules) installs a high-priority prefilter that disables every non-neural symbol on an ANN-Train scan, so a training vector is built without issuing RBL/DNS lookups, fuzzy checks, Bayes classification, or ClickHouse work \u2014 with the stored vector and profile key byte-for-byte identical to the live full-scan path, and an automatic fallback to a full scan if any applicable rule needs symbols. `train.frozen` stops automatic training and auto-storing of live vectors, so a frozen model's training pools cannot drift, while inference keeps serving the current ANN; an explicit ANN-Train request still stores and retrains on demand. A supporting `task:disable_all_symbols([skip_mask])` Lua binding is also added","**External rspamadm command discovery**: `rspamadm` now discovers Lua sub-commands beyond the built-in directory \u2014 from the `rspamadm.d` drop-in directory under the configuration root (consistent with `local.d`), and from every directory listed in the colon-separated `RSPAMADM_COMMAND_PATH` environment variable. Duplicate command names are skipped with first-wins ordering, so built-in commands can never be shadowed, and externally loaded modules run with the same globals and Lua path as built-in ones. Third-party or premium packages can now ship `rspamadm` commands without writing into the rspamd-owned lualib tree","**rspamadm password from the environment**: The controller password can be supplied via the environment instead of an interactive TTY prompt, so automation (for example, a continuous-deployment pipeline verifying a clean state) can drive password-protected `rspamadm` operations non-interactively ([#6114](https://github.com/rspamd/rspamd/pull/6114))","**Symcache flags in configdump**: `rspamadm configdump --symbol-details` now reports a `flags` array per symbol (`fine`, `empty`, `explicit_disable`, `explicit_enable`, `ignore_passthrough`, `nostat`, `idempotent`, `mime`, `trivial`, `skip`, `composite`, `ghost`, `coro`), making it possible to check \u2014 for instance \u2014 whether a symbol requires explicit enabling under the new settings policy; structural types remain in the `type` field"]},{title:"Changed",items:["**'orig' address flavour returns wire addresses only** (incompatible): `task:get_from({'mime', 'orig'})` previously returned both the preserved original addresses *and* the rewritten ones appended by `task:set_from`, making the flavour unusable for consumers that need the message as it arrived \u2014 a duplicate-From check would misfire on the combined list, and Lua could not tell the entries apart. Now, when originals were preserved by a rewrite, only the original addresses are returned; without a rewrite the behaviour is unchanged. The `aliased` and `original` email-address flags are exposed to Lua, and the flavour is reachable from selectors as `from('mime', 'orig')`. This rework underpins the DMARC/SPF sender-identity fixes below ([#6141](https://github.com/rspamd/rspamd/pull/6141))","**WebUI tables migrated from FooTable to Tabulator** (incompatible): Every WebUI table \u2014 errors log, throughput summary, symbols, history, and scan results \u2014 now uses Tabulator, and the vendored FooTable library is removed. FooTable is unmaintained (last release 2017) and jQuery-bound, while Tabulator is vanilla JS and survives the planned jQuery removal. The migration brings per-column header filters on all tables, a global boolean search box for history/scan with the familiar query language (whitespace = AND, `OR`, `-` to exclude, quoted exact phrases) that combines with the column filters, persisted column visibility, and theming mapped to Bootstrap CSS variables so light and dark modes flip automatically ([#6120](https://github.com/rspamd/rspamd/pull/6120))"]},{title:"Fixed",items:["**Alias rewriting no longer touches From domains**: The From address is the sender's identity \u2014 SPF, DKIM, and DMARC all key on its domain \u2014 yet a virtual alias mapping to another domain could rewrite it, breaking authentication of perfectly legitimate mail. Service rules and alias resolution may now only touch the local part of the From address; any rewrite that would change the domain is discarded. Recipient rewriting is unaffected, since recipients are interpreted by the receiving host and no authentication protocol keys on them. A spurious `task:set_from` call is also fixed: a rewrite of any earlier address (such as an envelope recipient) previously caused the unmodified MIME From to be marked as aliased and duplicated ([#6137](https://github.com/rspamd/rspamd/issues/6137))","**googlemail.com is never rewritten to gmail.com**: These are DNS-distinct domains with independent SPF and DMARC records (gmail.com publishes `p=none` while googlemail.com publishes `p=quarantine`), so substituting one for the other broke DKIM alignment, changed the effective DMARC policy, and misattributed DMARC reports. The gmail-style user-part canonicalization (dots and plus tags) is kept for googlemail.com addresses, but the domain is preserved; the equivalence remains available in `lua_util.remove_email_aliases` for dedup-only consumers such as email-hash RBL lookups ([#6137](https://github.com/rspamd/rspamd/issues/6137))","**DMARC evaluates the message as transmitted**: DMARC must align identifiers and look up the policy for the RFC5322.From domain as it appears in the message, and check SPF alignment against the RFC5321.MailFrom as transmitted. The module now uses the 'orig' address flavour, so rewrites done via `task:set_from` (for example by the aliases module) no longer affect alignment, policy lookup, or rua report attribution ([#6137](https://github.com/rspamd/rspamd/issues/6137))","**SPF checks the original envelope sender**: SPF evaluates the RFC5321.MailFrom as it was transmitted, including local-part macros, so the SPF record and credentials are now resolved from the original envelope sender even when the envelope from has been rewritten ([#6137](https://github.com/rspamd/rspamd/issues/6137))","**Forged recipients compares addresses at the same altitude**: The rule compared the rewritten envelope view against the wire MIME headers, so a recipient alias mapping to another domain \u2014 legal for recipients \u2014 could make a perfectly matching To header look forged. Both the envelope and the headers are now compared as they were seen in the message, restoring what the rule checked before alias rewriting was applied to tasks ([#6137](https://github.com/rspamd/rspamd/issues/6137))","**Semicolon-separated address lists**: A semicolon is not an RFC 5322 address-list separator (it only terminates a group construct), but Outlook-style `;`-separated recipient lists are pervasive in real mail and most MTAs split them. Previously such a list was parsed as a single malformed address whose local part swallowed the semicolon and the next address, breaking recipient counts and producing false invalid-address signals. `;` is now treated exactly like `,`, mirroring the comma behaviour in every edge case: semicolons inside quoted strings are preserved, `undisclosed-recipients:;` yields no addresses, and a group's trailing `;` no longer leaks into the last member's domain ([#6143](https://github.com/rspamd/rspamd/pull/6143), [#6139](https://github.com/rspamd/rspamd/issues/6139))","**Fuzzy hashing skips injected parts**: Parts injected into the task by computation, rather than present in the original message, are now excluded from fuzzy hashing on both learn and check, so fuzzy storage operates only on genuine message content","**Phishing cross-TLD brand false positives**: Displaying `brand.co.uk` over a `brand.com` link no longer fires PHISHING at full weight. Registrable labels are now compared instead of stripping only the last dot component (the old code handled only single-label suffixes, so `brand.co.uk` vs `brand.com` was Levenshtein-compared as full strings); the DMARC-authenticated-sender exclusion now normalises the From domain to its eSLD so it actually matches when DMARC reports a subdomain; and `strict_domains` is checked regardless of the computed weight, so displaying a strictly protected domain over a same-label, different-suffix target still fires","**Glob map patterns anchored to the whole subject**: Glob map entries were compiled into unanchored regexps with substring-search semantics, so a `t.co` entry matched `walmart.com` and `*.bit.ly` matched `foo.bit.ly.evil.com`. Translated patterns are now wrapped in `^(?:...)$` at map load time: bare names match exactly, wildcards match only what they say. This affects all glob maps \u2014 multimap `glob`/`glob_multi`, url_redirector `redirector_hosts_map`, dkim_signing/arc signing and key tables, mx_check exclusions, and the rbl glob returncodes matcher. Maps that relied on the accidental substring behaviour must now use explicit wildcards ([#6125](https://github.com/rspamd/rspamd/issues/6125))",'**Selectors must parse to the end of input**: The selector grammar was not anchored, so lpeg matched the longest valid prefix and silently dropped the rest \u2014 `from("smtp"):domain:lower` was accepted and evaluated as `from("smtp"):domain`, and trailing garbage after a selector or a `;` list element was ignored across every consumer, including the WebUI\'s selector checker. Parsing now requires the whole input to be consumed and reports the position and the unparsed tail otherwise. Selectors that previously loaded thanks to the silent truncation now fail configuration load with a pointed error; they were never evaluating as written, so failing loudly is the correct behaviour ([#6127](https://github.com/rspamd/rspamd/pull/6127))','**Selector method calls on missing methods no longer crash the scan**: A selector like `time:digest` \u2014 method syntax used where a transform was meant \u2014 aborted the whole selector at scan time with an uncaught "attempt to call a nil value" error. The method lookup is now guarded: the selector yields no value and logs an error that hints at the `.name` transform syntax when the name matches a known transform; valid method calls such as `:lower` are unaffected',"**secretbox out-of-bounds nonce read**: Both `lua_cryptobox` secretbox encrypt and decrypt built a zero-padded 24-byte nonce but then passed the original short nonce pointer to libsodium, reading up to 23 bytes past the buffer and producing ciphertexts that depended on adjacent memory contents. The padded nonce is now passed, so a short nonce produces the same ciphertext as its explicitly zero-padded form, and the two forms decrypt each other ([#6121](https://github.com/rspamd/rspamd/issues/6121))",'**lua_kann load and save**: `rspamd_kann.load({filename = ...})` always failed with "missing filename" because the options table was read from the wrong argument index (a copy-paste from `save`, where argument 1 is `self`), and `save({filename = ...})` returned the filename string instead of `true` because a trailing pop removed the boolean result it had just pushed. Both forms now work, with a regression test covering the file round-trip',"**Duplicate GPT request with synchronous context_augment**: When a `context_augment` function invoked its callback synchronously, the pending-fetches counter dropped to zero inside the callback and triggered the request; control then returned to the main flow, where a now-redundant guard fired a second request \u2014 this time without the context snippet. The duplicate path is removed ([#6142](https://github.com/rspamd/rspamd/pull/6142))","**ICAP HTTP header insertion**: Fixed incorrect insertion of an HTTP header when the ICAP module assembles the request headers in `get_http_headers` ([#6117](https://github.com/rspamd/rspamd/pull/6117))","**dmarc_report batch throttling**: Report sending no longer calls a blocking sleep from an async callback when throttling batches, which stalled the event loop instead of pacing the sends ([#6132](https://github.com/rspamd/rspamd/pull/6132))","**Lua 5.5 compatibility**: Lua 5.5 makes `for`-loop control variables read-only, turning assignments to them into compile-time errors. The most visible casualty was the bundled lupa Jinja template engine, which failed to load with \"attempt to assign to const variable 'word'\" and made rspamd unable to start when configs use Jinja templating. All remaining offenders across the tree \u2014 found by parse-checking with luac 5.5 \u2014 now rebind the value to a local instead ([#6130](https://github.com/rspamd/rspamd/pull/6130))"]},{title:"Security",items:["**message/rfc822 recursion depth (remote DoS)**: A chain of bare `Content-Type: message/rfc822` wrappers recursed through the MIME parser's message branch without ever tripping the `max_nested` (64) limit: unlike the multipart branch, the message branch never incremented the nesting counter actually carried into the recursion, so the entry guard never fired. Recursion was bounded only by message size \u2014 about 34 bytes per level \u2014 so a deeply nested message could exhaust the worker stack (a remote, unauthenticated denial of service via HTTP submission or SMTP/milter delivery), and the per-level boundary pre-scan made even non-crashing depths quadratic in CPU. The message branch now mirrors the multipart accounting, capping recursion at `max_nested`, which also caps the number of preprocess passes and eliminates the quadratic-CPU vector; a regression test feeds a 500-level chain and asserts the parsed part count stays capped. Reported by @gronke"]}],summary:"This patch release fixes a critical remote denial-of-service in the MIME parser via deeply nested `message/rfc822` messages and repairs a cluster of sender-authentication bugs where alias rewriting leaked into DMARC alignment, SPF evaluation, and forged-recipient checks \u2014 including the long-standing googlemail.com\u2192gmail.com rewrite that silently changed the effective DMARC policy. It also anchors glob map patterns and selector parsing (both may require config fixes where the old lax behaviour was relied upon), adds per-symbol Lua conditions for composites, a settings apply policy for explicit-enable symbols, the static_embed neural provider, and completes the WebUI migration from FooTable to Tabulator. Recommended upgrade for all users due to the MIME parser fix."},{version:"4.1.1",date:"2026-06-19",type:"patch",title:"Patch Release with checkv3 Content Negotiation, Neural Extensibility, CSS Hidden-Text Detection, and 4.1.0 Regression Fixes",sections:[{title:"Added",items:["**/checkv3 content negotiation**: The `/checkv3` reply now honours the client's `Accept` and `Accept-Encoding` headers at the single `reply_v3` chokepoint shared by the scan worker, `rspamd_proxy`, and the controller. `application/json` / `application/msgpack` yield a plain v2-style single-body reply, `message/rfc822` a `multipart/mixed` envelope, and `multipart/form-data` (also the default for an absent or wildcard `Accept`) a true form-data envelope; an `Accept` listing only unsupported types returns `406 Not Acceptable`. Compression is negotiated separately \u2014 `zstd` when offered, identity otherwise \u2014 and `Vary: Accept, Accept-Encoding` is always advertised. `rspamc` now requests `multipart/form-data` and accepts any `multipart/` subtype ([#6083](https://github.com/rspamd/rspamd/pull/6083))","**CSS hidden-text detection**: The invisible-text detector learns several more CSS tricks used to dilute visible spam with hidden ham text \u2014 off-screen positioning (`position:absolute|fixed` with a large negative `left`/`top`), image-replacement `text-indent`, `clip` / `clip-path` collapsing the element to zero area (`rect(0,0,0,0)`, `inset(100%)`, `circle(0)`), `visibility:collapse`, and tiny font sizes (\u2264 3px). These are now modelled as a hidden display so the effect correctly propagates to descendants, replacing a fragile substring heuristic that only bumped a feature counter and never actually hid the text","**Zip-bomb extraction limits**: `lua_archive` (`unpack` / `unzip` / `untar`) gains an optional `opts` table that caps extraction while reading, so a tiny hostile archive can no longer expand to gigabytes and OOM the worker: `max_output` (total uncompressed bytes), `max_file_size` (per-member cap, truncated at the cap), `max_files` (member count), and `max_ratio` (per-member uncompressed/compressed ratio). The ratio uses real compressed bytes consumed, checked per chunk above a 64 KiB floor, so a bomb is stopped early even with no size cap set. All limits are opt-in (0/absent = unlimited), preserving the previous behaviour, and a second boolean return flags truncation so a capped extraction is never mistaken for a complete one","**Byte-distribution statistics for rspamd_text**: New methods on the `rspamd_text` class expose ENT-style byte statistics for content analysis \u2014 `entropy()`, `byte_mean()`, `byte_deviation(mean)`, `serial_correlation()`, and `monte_carlo_pi()`. Each takes an optional 1-based `(start, len)` range (consistent with the rest of the slicing API), runs O(n) over a single shared histogram pass, and produces deterministic, bit-reproducible results ([#6106](https://github.com/rspamd/rspamd/pull/6106))",'**zlib/YARA-compatible CRC-32**: Standard CRC-32 (poly `0xEDB88320`) is now available as `rspamd_text:crc32([start[, len]])`, `rspamd_util.crc32(input[, start, len])`, and a streaming `rspamd_cryptobox_hash.create_specific("crc32")`, bit-exact with `zlib crc32()` and YARA\'s `hash.crc32` (e.g. `"123456789"` \u2192 `0xCBF43926`). The text/util forms return a Lua integer over a zero-copy 1-based slice with no buffer copy',"**Explicit multipattern SOM flag**: `RSPAMD_MULTIPATTERN_SOM` (`rspamd_trie.flags.som`) explicitly requests start-of-match offsets, overriding the incompatible single-match flag. The offset convention is now documented \u2014 1-based pattern id, 0-based byte offsets, start inclusive and end exclusive (so `end - start` is the match length) \u2014 and the regex fallback used when hyperscan is unavailable now reports the true start/end from the PCRE search instead of a bogus `end - strlen(pattern)` for variable-length matches ([#6105](https://github.com/rspamd/rspamd/pull/6105))","**Pluggable neural registries**: The neural plugin becomes an extension point so third-party (including closed-source) modules can add feature providers and network topologies without patching the core. `register_architecture(name, builder)` / `get_architecture(name)` join the existing `register_provider`; the built-in `symbol`, `embedding`, and `conv1d` architectures are now registered through it, `create_ann()` dispatches on `rule.architecture` and falls back to the historical auto-selection when it is unset (so existing configs are unaffected), and an unknown architecture now fails loudly with a hint that the providing module may not be loaded",'**fasttext_embed sequence output**: The `fasttext_embed` provider gains `output_mode = "sequence"`, emitting the first `max_words` word vectors flattened word-major and zero-padded to `max_words * channels`, so custom architectures can learn their own pooling instead of receiving a pre-pooled vector. An optional `word_selection = "sif"` fills the sequence with the most distinctive (highest SIF weight) words from anywhere in the message rather than the leading ones; the default stays `prefix`',"**Multi-head attention pooling**: A new `attn_pool` KANN operator performs multi-head dot-product attention pooling over a zero-padded sequence of word vectors with learned query vectors, masking all-zero padding positions out of the softmax. Exposed as `rspamd_kann.layer.attn_pool(node, n_words[, n_heads])`, it converges on a word-order-invariant needle-in-haystack task that a flat dense net cannot solve, and the operator is appended to preserve model serialization compatibility","**KANN slice and concat transforms**: `rspamd_kann.transform.slice(node, axis, start, end)` (0-based, end exclusive) and `rspamd_kann.transform.concat(axis, node1, node2, ...)` expose the already-serialized `kad_slice` / `kad_concat_array` graph operators to Lua, making split/bypass/merge topologies \u2014 e.g. routing parts of a fused input vector through different sub-networks for attention pooling with late fusion \u2014 buildable from Lua"]},{title:"Fixed",items:["**DNS resolver startup crash (4.1.0 regression)**: A nameserver that failed to resolve at config time was turned into a zero-address pending-resolve upstream and then dereferenced as NULL in `rspamd_dns_server_init`, crashing the worker with SIGSEGV at startup. Resolver nameservers are consumed synchronously and can never be promoted asynchronously, so they are no longer deferred, with a NULL guard added as defense in depth ([#6096](https://github.com/rspamd/rspamd/issues/6096))","**Milter QUARANTINE with custom reply (3.10.0 regression)**: The QUARANTINE command was emitted only inside the branch that synthesises a default quarantine reason, so a caller-supplied SMTP message (e.g. `task:set_pre_result('quarantine', 'reason')`) suppressed the command entirely and the message was accepted instead of quarantined. The supplied message now becomes the quarantine reason, matching the reject and tempfail branches; this affects both `METRIC_ACTION_QUARANTINE` and reject converted via `quarantine_on_reject` ([#6088](https://github.com/rspamd/rspamd/issues/6088))","**Coroutine thread-pool safety**: Async Lua libraries (DNS, Redis, TCP, HTTP, util) capture the running coroutine at a yield point and resume it later from a C completion callback, with nothing previously guaranteeing the resumed entry was still the captured one. Each pooled thread now carries an explicit lifecycle and generation counter, and the entries are refcounted, so a double-fired event, a completion racing task teardown, or an entry recycled into another task either resumes the correct live coroutine or becomes a logged no-op \u2014 instead of dereferencing freed memory ([#6104](https://github.com/rspamd/rspamd/pull/6104))","**lua_worker subprocess deadlock**: A function run via `worker:spawn_process` that returned `nil` (or any non-string value) logged an error but wrote nothing to the result pipe, so the parent waited forever for a reply while the child blocked on the post-reply ack \u2014 deadlocking both processes and anything serialised behind them, notably the neural training lock. Invalid return values are now reported to the parent as a regular error reply, so `on_complete` fires and the caller can recover","**Neural training on dense embeddings**: Training on dense provider features (`fasttext_embed`, `text_hash`) with the historical `learning_rate = 0.01` could drive the net into tanh saturation and silently save a degenerate single-class model that classified every message the same way until the next retrain \u2014 roughly one weight-init in three on a real corpus. Dense-feature rules now use the funnel (embedding) architecture, whose layernorm fixes the conditioning; the learning-rate default is resolved by input type (0.001 for dense embeddings, 0.01 for symbol vectors, an explicit config value still wins); and a quality gate rejects a constant/single-class model so training retries on the next cycle with a different init","**Stranded trained ANNs**: A trained ANN could become unreachable to workers \u2014 `NEURAL_SPAM` / `NEURAL_HAM` stopped firing while the controller logged a version mismatch forever \u2014 because a stale, empty, high-version profile entry shadowed the live lower-version blob and, with no TTL, never expired. New versions are now seeded monotonically from the profile actually trained from, selection falls back to the next compatible profile with a live blob (with a throttled warning) when the chosen one is missing, and profile/blob entries get a TTL refreshed each cycle so an actively used ANN never expires out from under its entry ([#6089](https://github.com/rspamd/rspamd/pull/6089))",'**mx_check loopback classification**: A domain whose MX resolves only to loopback is hosted on the scanning host itself (typically the host\'s own FQDN mapped to `127.0.0.1` in `/etc/hosts`), but it scored `MX_BOGON_ONLY` (+8.0) \u2014 the strongest "not spam infrastructure" signal treated as the strongest spam signal, penalising fully DMARC-aligned self-hosted mail. `127.0.0.0/8` and `::1/128` move from the bogon set to the local set, so such mail now emits `MX_LOCAL_ONLY` (3.0) ([#6101](https://github.com/rspamd/rspamd/issues/6101))',"**Monitored random DNS prefixes**: `random_monitored` RBL checks built random labels from an alphabet including `-` and `_`, producing names like `_Q8...0-` or `-7d0...` that are not valid DNS labels (RFC 952/1123) and that authoritative DNSBL servers such as spfbl.net reject with SERVFAIL. The alphabet is restricted to alphanumerics, which always forms a valid label regardless of position while keeping ample entropy ([#6103](https://github.com/rspamd/rspamd/issues/6103))","**v3 multipart boundary case**: RFC 2046 boundaries are case-sensitive, but the v3 HTTP multipart callers used the boundary lowercased for MIME-client quirks, so requests with uppercase boundary characters failed to parse. The case-preserving `orig_boundary` is now used in `protocol.c`, `rspamd_proxy.c`, and `rspamdclient.c`","**/checkv3 on the controller**: The controller's HTTP path router never registered `/checkv3`, so a plain `rspamc --protocol-v3` \u2014 which defaults to the controller port 11334 on localhost \u2014 returned 404. The endpoint is now registered and routed to the existing scan handler, branching on `CMD_CHECK_V3` in both directions to mirror the proxy, with an auth posture matching `/check` and `/checkv2` ([#6083](https://github.com/rspamd/rspamd/pull/6083))","**Symcache timeout inflation**: A `saved_priority` initialised to `-1` split the first item of each pre/postfilter/idempotent phase off from its priority group and counted it individually, inflating the computed maximum symbols-cache timeout. It is now seeded from the first item's priority so items at the same priority group correctly ([#6094](https://github.com/rspamd/rspamd/pull/6094), [#6092](https://github.com/rspamd/rspamd/issues/6092))","**Query-embedded URLs with '='**: A bare embedded URL is its own query parameter and can itself contain `=` (e.g. base64 padding in the path), but the query-embedded scan treated everything before the first `=` as a parameter key and discarded such URLs. The prefix is now treated as a key only when it has no URL structure characters (`:` or `/`); otherwise the whole parameter is scanned","**URLs with multiple '@' and backslashes**: URLs containing several `@` signs and backslashes were multiplied into many spurious URLs. Long legitimate conversations can accumulate such links, so the spurious expansion could drop mail and, with autotrain enabled, train the system badly; those URLs are no longer multiplied ([#6065](https://github.com/rspamd/rspamd/pull/6065))","**More CSS hiding via overflow/opacity/max-***: Phishing that dilutes visible content with hidden ham text using `max-width:0;max-height:0;overflow:hidden`, `opacity:0`, or `height:0` was missed \u2014 `max-*` and `overflow` were unparsed, `opacity` was parsed but discarded, and a copy-paste bug applied `height:0` to the block width. These properties are now parsed, a zero-dimension-with-`overflow:hidden`, low-opacity, or hidden-ancestor block is treated as invisible and propagated to descendants, and a child display value can no longer resurrect content hidden by an ancestor","**OpenSSL 4.0 build**: OpenSSL 4.0 made `ASN1_STRING` (and thus `ASN1_OCTET_STRING`) opaque, so direct access to its length/data fields no longer compiled; `ASN1_STRING_length()` / `ASN1_STRING_get0_data()` (available since OpenSSL 1.1.0 and LibreSSL 2.7) are used instead, and the legacy OpenSSL init calls are moved under the pre-1.1.0 guard to fix no-deprecated builds ([#6087](https://github.com/rspamd/rspamd/issues/6087))","**doctest 2.5.0 build**: `mime_string`'s iterator is intrinsically const, so it is marked as such and the `const_iterator` reference is dropped, fixing a compile error where doctest 2.5.0 receives a const reference and cannot use a mutable iterator ([#6072](https://github.com/rspamd/rspamd/pull/6072))","**Bundled simdutf headers**: The bundled simdutf headers are now prioritised over system ones to avoid version skew ([#6082](https://github.com/rspamd/rspamd/pull/6082))"]}],summary:"This patch release fixes a critical 4.1.0 regression that crashed workers at startup when a DNS nameserver failed to resolve, restores milter QUARANTINE handling broken since 3.10.0, and hardens the async Lua coroutine pool against use-after-free on completion races. It also adds `/checkv3` content negotiation via `Accept`/`Accept-Encoding`, opt-in archive extraction limits against zip bombs, broader CSS hidden-text detection, and an extensibility layer for neural feature providers and ANN architectures (including multi-head attention pooling), alongside several neural training-stability fixes. Recommended upgrade for all users, and especially for anyone running 4.1.0."},{version:"4.1.0",date:"2026-06-05",type:"major",title:"Major Release with Load-Aware Upstreams, MX Check Rework, Dynamic Composites, and Hardening",sections:[{title:"Changed",items:["**MX check rework**: `mx_check` replaces its single domain-keyed cache with a three-layer Redis design (`d:<domain>` / `m:<mxhost>` / `i:<ip>` under `<key_prefix>:`), so two domains sharing an MX host (every G-Suite/M365 tenant, every ESP customer) reuse the m- and i-layer entries and hit cache with zero new DNS or TCP work. Outcome symbols are finer-grained and `MX_NONE` now replaces the old `MX_NXDOMAIN`/`MX_MISSING`; the probe is split into clean connect-only and full SMTP-banner shapes with multi-line greeting support. **Operators should review their `mx_check` scores and any rules referencing the old symbol names** ([#6055](https://github.com/rspamd/rspamd/pull/6055), [#6032](https://github.com/rspamd/rspamd/issues/6032))","**Fuzzy SRV discovery by default**: The stock `rspamd.com` fuzzy rule now uses `service=fuzzy+rspamd.com` SRV-based discovery instead of a hardcoded round-robin host list, so backends and ports are managed entirely in DNS. The legacy `fuzzy1`/`fuzzy2` hostnames keep resolving to every live backend, so installs that pinned the old string are unaffected"]},{title:"Added",items:["**Custom metadata for /checkv3**: A `headers` sub-object in the multipart metadata part is injected into the task request headers (so `task:get_request_header()` works for custom fields), and the full metadata object is exposed to Lua via `task:get_metadata()` / `task:get_metadata_field(key)`. Both paths travel in the multipart body, free of the 80KB HTTP header limit that v2 hits; `rspamc` gains a repeatable `--metadata-header KEY=VALUE` option ([#6074](https://github.com/rspamd/rspamd/pull/6074))","**Dynamic composites**: A reserved `dynamic` key in the `composites { ... }` block attaches a hot-reloadable map of composites (file, URL list, or signed map) using the same vocabulary as static composites. Reloads register new names with the symcache, materialise removed names as stubs, and swap an atomic generation so in-flight tasks keep their snapshot ([#6064](https://github.com/rspamd/rspamd/pull/6064))","**Bulk and regexp symbol lookups**: `task:has_symbol()` / `task:get_symbol()` accept a `{S1, ..., Sn}` table form, and new `task:has_symbol_regexp(re)` / `task:get_symbol_regexp(re)` match fired symbol names against an `rspamd_regexp` userdata","**Phase-specific TCP timeouts**: `rspamd_tcp.new` gains `connect_timeout` / `read_timeout` / `write_timeout` for independent per-phase budgets and an `on_error(err, conn)` callback that fires once for pre-connect failures (DNS, socket, connect refused/timeout, SSL handshake). Legacy single-`timeout` callers keep their existing deducted-budget contract unchanged ([#6034](https://github.com/rspamd/rspamd/pull/6034))",'**Stalled-scan diagnostics**: On task timeout the log now includes a grouped summary of still-pending async events (DNS / Redis / Lua HTTP / fuzzy / TCP) annotated with the stalling rule and operation, plus the list of symbols that started but never finished \u2014 replacing the opaque "forced processing" line with an immediate picture of what hung the scan ([#5990](https://github.com/rspamd/rspamd/pull/5990))',"**Load-aware upstream selection**: Upstreams now support Power of Two Choices selection, a per-upstream latency EWMA with configurable half-life, and linear slow start on revive to avoid thundering-herd flap loops. `RSPAMD_UPSTREAM_RANDOM` callers are transparently upgraded to P2C ([#6013](https://github.com/rspamd/rspamd/pull/6013))","**Per-target SRV upstreams**: Each SRV reply target now materialises its own `struct upstream` with its own error budget, weight, latency EWMA, and address list, so SRV weights are honoured and a single failing target no longer drains the whole cluster's budget ([#6030](https://github.com/rspamd/rspamd/pull/6030))","**Deferred DNS resolution**: A transient DNS failure at config time no longer drops an upstream and cascades into module init failures. Unresolvable hostnames are kept pending and retried by the async lazy-resolve machinery (with exponential backoff), so the daemon starts and recovers automatically without a restart ([#6008](https://github.com/rspamd/rspamd/pull/6008), [#6000](https://github.com/rspamd/rspamd/issues/6000))","**MX check classification and trust maps**: `mx_check` partitions resolved MX-target IPs into PUBLIC / LOCAL / BOGON (only PUBLIC is probed), adds `bad_mxs` (glob on hostnames) and `bad_ips` (radix on IPs) punishment maps with optional per-entry weight multipliers, per-source checks across envelope / reply-to / MIME-from, and `check_authorized` / `check_local` run-scope toggles ([#6039](https://github.com/rspamd/rspamd/pull/6039), [#6032](https://github.com/rspamd/rspamd/issues/6032))","**External-service anchor symbols**: Every external service is scheduled under a stable `<RULE>_CHECK` callback symbol so it is a predictable dependency target regardless of how the scan-result symbols are named, generalising the existing `VADE_CHECK` / `CLOUDMARK_CHECK` pattern","**Chain-aware URL redirector cache**: `url_redirector` gains a per-hop Redis cache with intermediate-hop injection, so shared intermediate links are reused across chains and resolved hops are made visible to downstream modules; the walk self-heals on partial cache misses and surfaces previously-silent lock/stale conditions ([#6014](https://github.com/rspamd/rspamd/pull/6014))","**Stealth fingerprint profiles**: `url_redirector` replaces its flat `default_ua` list with five coherent browser profiles (Chrome, Edge, Firefox, Safari), each bundling a User-Agent with the exact header set, values, and order that browser sends (including `sec-ch-ua` client hints where appropriate). One profile is picked per task and reused across every hop ([#6053](https://github.com/rspamd/rspamd/pull/6053))","**Glob redirector hosts**: `redirector_hosts_map` now accepts glob patterns (e.g. `*.bit.ly`, `*.t.co`); bare hostnames still match exactly, so existing maps are unaffected ([#6056](https://github.com/rspamd/rspamd/pull/6056))","**GET for selected redirector URLs**: `url_redirector` can issue GET (rather than HEAD) for an operator-defined list of URLs matched by regexp ([#6043](https://github.com/rspamd/rspamd/pull/6043))","**Insertion-ordered HTTP headers**: An opt-in `RSPAMD_HTTP_FLAG_ORDERED_HEADERS` emits headers in insertion order instead of hash order; `lua_http` accepts a list form (`{{'name','value'}, ...}`) to preserve order, used by the redirector stealth profiles","**Richer Elasticsearch logging**: The `elastic` module now logs Reply-To user/domain, received IPs, per-URL and CTA URL metadata via a new `collect_urls` block, and the forcing module name from `task:has_pre_result()` ([#6018](https://github.com/rspamd/rspamd/pull/6018))","**ClickHouse column presets**: Named `extra_columns` presets, including an initial outbound preset, can be selected without hand-rolling the schema ([#5983](https://github.com/rspamd/rspamd/pull/5983))","**New selectors**: `fuzzy_digest`, `fuzzy_shingles`, `authenticated`, and `received_count` ([#5981](https://github.com/rspamd/rspamd/pull/5981))","**HTML5 tag definitions**: 32 modern HTML5 tags (sectioning, media, text-level, interactive, forms, web components), notably `video`/`audio`/`source`/`track`/`picture`/`svg`, so their URLs and structure are visible to the parser; new tag IDs are appended so existing IDs stay stable","**DMARC report throttling**: `rspamadm dmarc_report` gains `-w`/`--batch-wait` to pause between batches (and stagger report generation), avoiding overload of the SMTP server and a weak resolver ([#5985](https://github.com/rspamd/rspamd/pull/5985))","**autolearnstats sorting**: `--sort-by <col>` and `--group` options for the `rspamadm autolearnstats` table output ([#6050](https://github.com/rspamd/rspamd/pull/6050))","**Feedback report parsers**: New `lua_feedback_parsers` lualib for parsing DSN (delivery status) and ARF (abuse) reports ([#5982](https://github.com/rspamd/rspamd/pull/5982))","**Structured custom-Lua loader**: `lua_extras` provides a two-phase loader for custom selectors, maps, and regexps under `lua.local.d/{maps,selectors,regexps}/`, resolving cross-kind dependencies (maps \u2192 selectors \u2192 regexps) so a selector can consume a map registered by an earlier kind ([#6020](https://github.com/rspamd/rspamd/pull/6020))","**eXpurgate scanner**: New `lua_scanners` engine for the eXpurgate anti-spam service ([#5755](https://github.com/rspamd/rspamd/pull/5755))","**Per-worker memory dumps**: New `rspamadm control memstat` command reports per-worker RSS, per-callsite mempool counters, Lua heap usage, and structured per-arena jemalloc stats, with `--short`, `--sort`, and per-section toggle flags ([#6016](https://github.com/rspamd/rspamd/pull/6016))","**Container-friendly baseline config**: The baseline pidfile and logging type/filename are now env-overridable (`RSPAMD_PIDFILE`, `RSPAMD_LOG_TYPE`, `RSPAMD_LOG_FILE`); an empty pidfile disables it (useful as PID 1), and `RSPAMD_LOG_TYPE=console` logs to stdout. Stock installs render the previous defaults bit-for-bit ([#6067](https://github.com/rspamd/rspamd/pull/6067))","**Auto-loaded fasttext model**: When no `fasttext_model` is configured, the shipped `$SHAREDIR/languages/fasttext_model.ftz` is loaded if present, so images bundling the model can drop the explicit override; stock installs without the file behave exactly as before ([#6067](https://github.com/rspamd/rspamd/pull/6067))","**Fixed-point float formatting**: `fpconv` gains `%.Nf` fixed-point formatting with correct rounding and carry handling ([#6061](https://github.com/rspamd/rspamd/pull/6061))"]},{title:"Fixed",items:["**Composite visibility from postfilters**: Composites depending only on ordinary filter-stage rules were wrongly deferred to the post-composites stage because nearly every virtual/callback symbol carries `NOSTAT`; a composite is now deferred only when a dependency actually resolves to the postfilter stage, restoring `task:get_groups()`/`get_symbols()` visibility from postfilters","**Ratelimit multi-bucket tracking**: A selector rule with several buckets (e.g. `200/1h` plus `30/1m`) keyed every bucket on the selector value alone, so only the last bucket was tracked and the other limits were silently ignored; each bucket now gets a distinct Redis key ([#6076](https://github.com/rspamd/rspamd/pull/6076), [#6059](https://github.com/rspamd/rspamd/issues/6059))","**Verbatim href preserved**: `url:get_raw()` returned the partially percent-decoded scratch buffer for HTML URLs; `url->raw` now points at a mempool-owned copy of the verbatim trimmed href ([#5986](https://github.com/rspamd/rspamd/issues/5986))","**Mailto canonicalisation**: A bare email in text/HTML and the same address inside an explicit `mailto:` URL were extracted as two separate emails; both injection sites now canonicalise to the slash-less `mailto:` form (RFC 6068) so dedup collapses them","**Long userinfo URLs preserved**: A blanket length REJECT silently dropped URLs whose userinfo exceeded 2KB \u2014 exactly the `https://legit.com   @evil.com/...` userinfo-obfuscation phishing pattern; the cap is raised to 16KiB and the URL is flagged obscured as soon as userinfo crosses 64 bytes",'**Naked-domain false positives**: `url_suspect` now requires a TLD of at least 3 chars for `word_dot` naked-domain matches, so prose like "pale blue dot so insignificant" no longer normalises to `blue.so`; explicit-protocol patterns still match two-char TLDs',"**Deterministic ARC header order**: `lua_mime.modify_headers` honoured its `order` list but serialised in hash order; ARC sets are now emitted in the conventional ARC-Seal / ARC-Message-Signature / ARC-Authentication-Results layout, which some validators (e.g. O365) require ([#6052](https://github.com/rspamd/rspamd/pull/6052), [#6045](https://github.com/rspamd/rspamd/issues/6045))","**DKIM permfail mapping**: An invalid DKIM record now maps to `dkim=permerror` in `Authentication-Results` instead of falling through to `dkim=none` ([#6028](https://github.com/rspamd/rspamd/pull/6028), [#5957](https://github.com/rspamd/rspamd/issues/5957))","**EAI Message-ID handling**: The `INVALID_MSGID` rule now honours `mime_utf8`, and the `enable_mime_utf8` option spelling is registered as an alias so it actually takes effect, fixing false positives on valid SMTPUTF8 Message-IDs (RFC 6532) ([#6011](https://github.com/rspamd/rspamd/pull/6011), [#6007](https://github.com/rspamd/rspamd/issues/6007))","**Fuzzy storage peer pipe**: Partial peer-pipe writes were retried from byte 0, shoving garbage into the pipe; writes now resume at the sent offset, and pending requests are drained on worker shutdown instead of leaking","**Fuzzy dynamic-ban leak**: Re-applying an already-present ban prefix orphaned a freshly allocated ban struct in the long-lived mempool on every refresh; the prefix is now looked up first and mutated in place on a hit","**Fuzzy persistent-TCP leak**: Per-frame `cmd_session` state (extensions buffer, key refs) leaked on every frame after the first on a persistent TCP connection; per-command state caching is dropped so each frame starts clean ([#6001](https://github.com/rspamd/rspamd/issues/6001))","**Allowed fuzzy clients on TCP**: Allowed clients are no longer blocked on the TCP path ([#5992](https://github.com/rspamd/rspamd/pull/5992))","**Neural ANN survival**: A trained ANN is now preserved across symbol-list drift and symcache-driven profile rotation; the profile digest is stabilised under `disable_symbols_input` (keyed on the providers config rather than the unrelated symbol catalogue), and training is retargeted to the newest profile so inference no longer goes dark for weeks until a fresh model trains ([#6041](https://github.com/rspamd/rspamd/pull/6041))","**Inline /checkv3 settings**: Inline `metadata.settings` on `/checkv3` were stashed directly on the task and skipped the apply pipeline; they now run through the same `settings.lua` apply path as v2, so action thresholds, symbols, subject, variables, and header edits take effect ([#5999](https://github.com/rspamd/rspamd/issues/5999))","**/checkv3 request headers**: Arbitrary client HTTP headers are now registered so `task:get_request_header()` works under v3, restoring v2 behaviour ([#5998](https://github.com/rspamd/rspamd/issues/5998))","**Token-bucket recovery**: A flapping upstream's token bucket drained monotonically toward zero and never recovered; lazy time-based refill restores tokens over wall time so a recovered upstream re-enters selection","**get_random termination**: `rspamd_upstream_get_random` looped forever when the only alive candidate matched the `except` argument; the empty and single-survivor cases are now front-gated","**Lua TCP leak**: A connection that read without writing leaked; the leak is closed ([#6048](https://github.com/rspamd/rspamd/pull/6048))","**Redis master under Sentinel**: Standalone `rspamadm` tools never resolved the current Redis master, round-robining writes that failed `READONLY` on replicas; a new one-shot `lua_redis.prepare_redis_setup` resolves the master (and loads scripts) for tools like `dmarc_report` ([#6015](https://github.com/rspamd/rspamd/pull/6015), [#6009](https://github.com/rspamd/rspamd/issues/6009))","**Elastic overflow guard**: The row-limit overflow guard called a non-existent `lua_util.newdeque()`; it now resets the buffer via the local `Queue` class","**DMARC report on PUC Lua**: The connect timestamp is floored before `os.date`, which PUC-Rio Lua rejects for non-integer floats (only seen on the Fedora build)","**Greylist timeout separation**: The greylisting period (5 min) was being picked up by `lua_redis` as the Redis connection timeout; a separate `redis_timeout` (default 1.0s) is introduced and propagated into nested `redis{}` blocks ([#5977](https://github.com/rspamd/rspamd/pull/5977))","**rspamadm vault output**: `rspamadm vault list` produced empty output for large vaults because the payload passed through a format-string logger; it is now written to stdout directly via `io.write` ([#6006](https://github.com/rspamd/rspamd/pull/6006), [#6005](https://github.com/rspamd/rspamd/issues/6005))","**Regexp capture groups**: An empty capture group in the middle of a match no longer discards all following groups (PCRE1 and PCRE2); results are truncated at the last non-empty group instead ([#5974](https://github.com/rspamd/rspamd/pull/5974), [#5973](https://github.com/rspamd/rspamd/issues/5973))",'**x-binaryenc charset**: ICU conversion is skipped for the synthetic `x-binaryenc` charset across all detection paths, silencing the spurious "cannot open converter" warning and correctly marking the part as raw binary ([#5984](https://github.com/rspamd/rspamd/pull/5984))',"**Timeout sanity check**: `configtest` now warns when `task_timeout` is less than a symcache symbol timeout, including per-worker overrides ([#5978](https://github.com/rspamd/rspamd/pull/5978))","**libc++ builds**: Use `string_view::data()` for pointer access where libc++ returns a wrapped iterator from `begin()`, fixing builds with libc++ 22 on FreeBSD ([#5969](https://github.com/rspamd/rspamd/issues/5969))","**In-place header rewrites**: `mime_headers`/`mime_encoding` now recompute lengths after in-place strip/trim rewrites, so stale trailing bytes are no longer pulled into the Message-ID or normalised charset name, and large-buffer offsets use `goffset` to avoid 32-bit truncation","**rfc2047 decode**: A failed base64 encoded-word no longer leaves uninitialised bytes in the decoded header value; the token length is reset to the saved offset on the failure path"]},{title:"Security",items:["**S/MIME NULL deref**: An S/MIME signed message wrapping a zero-length `pkcs7-data` OCTET STRING crashed the parser on the first byte check; the empty inner recursion is skipped and a defensive guard is added against NULL/empty buffers","**S/MIME recursion bound (DoS)**: Deeply nested `application/pkcs7-mime` layers re-entered the parser without incrementing the nesting counter, recursing to a depth bounded only by message size and exhausting the worker stack; the S/MIME re-entry is now accounted against `max_nested` and the CMS/PKCS7/BIO objects are freed on the error path","**MIME parser guards**: Additional defensive guards against NULL dereference (Content-Type iteration, malformed PKCS7 signed-data) and a leak of the recursive parser context on the early error-return path, plus a corrected `begin-base64` UUE prefix offset","**Nested-query URL DoS**: A crafted message with a few levels of percent-escaped nested query URLs exhausted the multipattern hyperscan scratch pool and aborted the worker via assertion; the scratch budget is enlarged, the query-nesting cap is restored to a fixed functional limit, and scratch exhaustion is made non-fatal ([#6066](https://github.com/rspamd/rspamd/pull/6066))","**Archive parser hardening**: RAR (v4/v5), ZIP, and 7-zip parsers are hardened against malformed attachments \u2014 re-validated filename lengths, guarded section/digest/bit reads, clamped extra-field advances, a fixed EOCD minimum, widened offset arithmetic against 32-bit wrap, and corrected 7-zip varint decoding (uninitialised value and an undefined-shift fix)","**Image linking guard**: Content-Id image linking is guarded against a NULL decoded header","**CSS out-of-bounds read**: The CSS ident-escape scanner could read one byte past a tightly-sized style-attribute buffer when a token ended in backslash plus a hex digit; the increment is now bounds-gated","**Header lookahead over-read**: `rspamd_string_find_eoh` peeked `p[1]` guarded only by `p < end`, reading one byte past the buffer on input ending in `\\r\\r`; it now checks `p + 1 < end`","**SPF over-read**: A TXT record of exactly `spf2.` advanced past one unvalidated byte and read past the string end; the parser now advances only past the validated prefix with short-circuiting checks","**DNS label overrun**: `rdns_parse_labels` never verified that label data fits within the packet, so a reply declaring more bytes than remained made the second-pass `memcpy` read past the (exactly-sized, on TCP) buffer; both plain and compressed labels are now validated, with an off-by-one fix in offset decompression","**HTML entity overflow**: In-place HTML entity decoding assumed the replacement is never longer than the source, which fails for short names expanding to multiple codepoints (`nGt`, `nLt`, `nvap`); the replacement is now bounds-checked against the remaining buffer","**Empty-host URL over-read**: `rspamd_url_maybe_regenerate_from_ip` could read `host[-1]` on an all-dots or zero-length-after-decode host; the trailing-dot loop condition is reordered and host length is re-checked after decoding","**Fuzzy network input hardening**: Three defensive fixes on user-controlled UDP/TCP paths \u2014 reset `msg_namelen` before every `recvmsg` to avoid parsing stale stack bytes, validate the reconstructed 14-bit TCP frame length before use, and clamp `n_extra_flags` before the fixed-size reply `memcpy`","**libucl upstream fixes**: Ported security fixes from libucl \u2014 msgpack negative fixint and unaligned-access fixes, a key-length validation, a nesting-depth limit, parser bounds checks, and a NULL guard in schema validators"]}],summary:"This major release reworks load-aware upstream selection (Power of Two Choices, latency EWMA, slow start, per-target SRV, deferred DNS) and the `mx_check` module (three-layer cache, finer outcome symbols, IP-class classification, trust maps), adds hot-reloadable dynamic composites, richer `/checkv3` metadata, phase-specific TCP timeouts, stalled-scan diagnostics, and numerous new features across logging, selectors, and tooling. It also lands an extensive round of memory-safety and DoS hardening across the MIME, archive, URL, DNS, HTML, SPF, and fuzzy-storage paths. Recommended upgrade for all users; operators of `mx_check` should review symbol names and scores, and Sentinel/SRV deployments will benefit from the upstream resilience improvements."},{version:"4.0.1",date:"2026-04-05",type:"patch",title:"Patch Release with Layered Settings Merge, Fuzzy Dynamic Blocks, and Critical Bug Fixes",sections:[{title:"Added",items:["**Layered settings merge**: Collect-then-merge flow replaces first-match-wins; layers are merged with per-field semantics (actions/scores override by higher layer, symbols union with conflict resolution); `force_actions` and `force_symbols` provide explicit override when layers conflict ([#5959](https://github.com/rspamd/rspamd/pull/5959))",'**Weak dependencies in symcache**: New `disabled` status distinguishes "disabled by settings" from "finished"; hard deps cascade-disable dependents while weak deps let them proceed \u2014 seven plugins annotated with correct dependency classification ([#5959](https://github.com/rspamd/rspamd/pull/5959))',"**Fuzzy dynamic block API**: `worker:block_fuzzy_client(addr, prefix_len[, expire_ts[, reason]])` Lua method for runtime IP/CIDR blocking in fuzzy storage without config changes or restarts; custom response codes \u2014 403 for ratelimits vs 503 for bans; `ratelimit_whitelist` check exposed to Lua ([#5962](https://github.com/rspamd/rspamd/pull/5962))"]},{title:"Fixed",items:["**Proxy milter fd leak**: Original fd was not closed after milter `dup()`, leaking one fd per milter connection and causing unbounded `CLOSE_WAIT` accumulation in production (regression in 4.0.0)","**Proxy self-scan task timeout**: Self-scan tasks incorrectly used the upstream wire timeout (e.g. 120s) instead of `task_timeout` (8s default), masking hung tasks","**ARC AAR header parsing**: Replace naive semicolon split with LPeg grammar that respects RFC 8601 comments `(...)` and quoted strings \u2014 fixes `cv=fail` on multi-hop ARC chains where `i=N` appeared inside comments ([#5963](https://github.com/rspamd/rspamd/issues/5963), [#5966](https://github.com/rspamd/rspamd/pull/5966))","**Dynamic ban expiry**: Fix `ev_now()` usage for correct monotonic time in dynamic block expiry","**Settings flow**: Fix dependency ordering in pre/postfilters and settings apply phase","**Redis backend build**: Add missing `#include <memory>` in `redis_backend.cxx` ([#5960](https://github.com/rspamd/rspamd/pull/5960))"]}],summary:""},{version:"4.0.0",date:"2026-03-28",type:"major",title:"Major Release with checkv3 Protocol, Built-in Fasttext, Multi-Flag Fuzzy Hashes, and Ring Hash Consistent Hashing",sections:[{title:"Breaking Changes",items:["**Bayes per-user resharding**: Jump Hash replaced with Ring Hash (Ketama) for consistent upstream hashing; per-user Bayes data on sharded Redis deployments will be on wrong shards after upgrade. Run `rspamadm statistics_dump migrate` before upgrading. Single-server deployments are unaffected. ([#5914](https://github.com/rspamd/rspamd/pull/5914), [4ea7504](https://github.com/rspamd/rspamd/commit/4ea750466))","**Content URLs included by default**: `include_content_urls` now defaults to `true`; URLs extracted from PDF and computed parts are returned by `task:get_urls()` by default, which may trigger new symbol hits on messages with PDF attachments. Restore old behavior with `include_content_urls = false` in `local.d/options.inc`. ([#5853](https://github.com/rspamd/rspamd/pull/5853))","**SSL worker option removed**: The `ssl = true` worker option has been removed; SSL is now auto-detected from bind socket flags. Remove `ssl = true` from worker configs and use the `ssl` suffix on bind lines instead. ([#5884](https://github.com/rspamd/rspamd/pull/5884))","**Proxy load balancing default changed**: Token bucket load balancing is now enabled by default for proxy upstreams, replacing simple round-robin. Remove the `token_bucket` key from proxy upstream config to restore round-robin behavior. ([#5874](https://github.com/rspamd/rspamd/pull/5874))","**SenderScore RBLs disabled by default**: `senderscore_reputation` is disabled by default as it requires a MyValidity account and was returning blocked results for all unregistered IPs. Users with registered accounts must explicitly re-enable the rule. ([#5907](https://github.com/rspamd/rspamd/pull/5907))","**DKIM unknown key handling per RFC**: Unknown and broken DKIM keys are now handled strictly per RFC, which may change DKIM results for messages with malformed keys. ([e9e6bac](https://github.com/rspamd/rspamd/commit/e9e6bac43))","**Suspicious TLDs now map-based**: The hardcoded suspicious TLD list has been replaced with `conf/maps.d/suspicious_tlds.inc`. Customize by creating `local.d/maps.d/suspicious_tlds.inc` (override) or `local.d/maps.d/suspicious_tlds.inc.local` (extend). ([614e68c](https://github.com/rspamd/rspamd/commit/614e68c8b))","**Neural module autolearn option renames**: Autolearn options in the neural module have been renamed to match RBL module naming conventions. Review custom neural configurations for use of old option names. ([#5835](https://github.com/rspamd/rspamd/pull/5835))","**libfasttext external dependency removed**: The external libfasttext C++ library has been replaced with a built-in mmap-based shim. The `ENABLE_FASTTEXT` cmake option is removed (always enabled). Packagers must remove the libfasttext build dependency. ([#5897](https://github.com/rspamd/rspamd/pull/5897))"]},{title:"Added",items:["**Jinja2 configuration templates**: Configuration files are now preprocessed by the [Lupa Jinja2-compatible template engine](/configuration/templates) before UCL parsing. Environment variables prefixed with `RSPAMD_` are exposed as the `env` table in templates; modified delimiters (`{= =}` for expressions, `{% %}` for control structures) avoid conflicts with UCL syntax. New validation filters (`mandatory`, `require_int`, `require_number`, `require_bool`, `require_duration`, `require_json`, `fromjson`) abort startup with a clear error on invalid input, enabling container-ready configuration validation without shell entrypoint scripts. ([#5938](https://github.com/rspamd/rspamd/pull/5938), [#5941](https://github.com/rspamd/rspamd/pull/5941))","**checkv3 multipart protocol**: New `/checkv3` endpoint using `multipart/form-data` requests and `multipart/mixed` responses; metadata sent as structured JSON/msgpack instead of HTTP headers, per-part zstd compression, optional body part for rewritten messages, and zero-copy piecewise writev for responses. Use `rspamc --protocol-v3` or `rspamc --msgpack` to activate. ([#5880](https://github.com/rspamd/rspamd/pull/5880))","**Pluggable Hyperscan cache backend**: Hyperscan compilation and caching moved to an async Lua backend with Redis-based shared database support across workers and hosts. Async compilation prevents blocking the main event loop; self-healing cache auto-detects stale blobs and triggers recompile; small databases compiled in-memory without file caching. ([#5813](https://github.com/rspamd/rspamd/pull/5813), [#5952](https://github.com/rspamd/rspamd/pull/5952))","**Multi-flag fuzzy hashes**: A single fuzzy hash can now carry up to 8 flags simultaneously, allowing multiple rules to match the same digest with independent flag/value pairs. Redis update path rewritten in Lua with EVALSHA and NOSCRIPT recovery. Backward-compatible epoch 12 wire protocol with highest-value flag promoted to the primary slot. Fuzzy hashes now stored in Redis history. ([#5894](https://github.com/rspamd/rspamd/pull/5894), [#5860](https://github.com/rspamd/rspamd/pull/5860))","**HTML fuzzy phishing detection**: Dual-mode fuzzy matching \u2014 template matching and domain-sensitive matching. New `FUZZY_HTML_PHISHING` symbol fires when an HTML template matches but domains differ, detecting reused phishing templates with swapped links. ([173058061](https://github.com/rspamd/rspamd/commit/173058061))","**Built-in Fasttext shim**: External C++ libfasttext replaced with a zero-dependency mmap-based reader providing shared memory across workers via `MAP_SHARED`, eliminating per-worker heap copies and saving approximately 500MB\u20137GB RAM. No more C++ exception ABI issues. Existing `.bin`/`.ftz` models continue to work unchanged. Fasttext wired through maps infrastructure for hot-reloading. ([#5897](https://github.com/rspamd/rspamd/pull/5897), [#5909](https://github.com/rspamd/rspamd/pull/5909))","**Neural network and LLM embedding improvements**: External pretrained neural model support; LLM embedding providers with multi-model support, mean+max pooling, and SIF word weighting; multi-layer funnel architecture; language-based model and URL selection; expression-based autolearn for neural LLM providers; GPT module with configurable consensus thresholds, `context_augment` hook, and mempool variable storage. ([#5924](https://github.com/rspamd/rspamd/pull/5924), [#5903](https://github.com/rspamd/rspamd/pull/5903), [#5897](https://github.com/rspamd/rspamd/pull/5897), [#5835](https://github.com/rspamd/rspamd/pull/5835))","**HTTPS server support**: Workers can now serve HTTPS natively with SSL auto-detected from bind socket configuration, enabling secure WebUI and API without a reverse proxy. ([#5884](https://github.com/rspamd/rspamd/pull/5884), [d04b367](https://github.com/rspamd/rspamd/commit/d04b367db))","**Ring Hash (Ketama) consistent hashing**: Proper consistent hashing with virtual nodes ensures only ~1/n keys redistribute when an upstream fails, and keys return to their original upstream on recovery. ([4ea7504](https://github.com/rspamd/rspamd/commit/4ea750466))","**Token bucket proxy load balancing**: New load balancing algorithm for proxy upstreams with configurable `max_tokens`, `scale`, and `base_cost` parameters for better burst traffic handling. ([#5874](https://github.com/rspamd/rspamd/pull/5874))","**Multiclass Bayes support**: Classifiers now support arbitrary classes beyond binary spam/ham. WebUI learning interface updated for multi-class workflows. `/stat` and `/bayes/classifiers` endpoints extended with classifier metadata. `rspamadm statistics_dump` supports multi-class dump and restore. ([#5900](https://github.com/rspamd/rspamd/pull/5900), [#5893](https://github.com/rspamd/rspamd/pull/5893), [#5914](https://github.com/rspamd/rspamd/pull/5914))","**Structured metadata exporter**: New structured formatter for the metadata exporter module with zstd compression option and detected MIME types for attachments. ([#5890](https://github.com/rspamd/rspamd/pull/5890))","**UUID v7 per task**: Native UUID v7 generation per scanning task synced with the `Log-Tag` header and ClickHouse UUID v7 column support. ([#5890](https://github.com/rspamd/rspamd/pull/5890))","**ARC trusted_authserv_id**: Reuse upstream authentication results via trusted `Authentication-Results` headers from known authentication servers. ([506ef44](https://github.com/rspamd/rspamd/commit/506ef44b8))","**Legacy protocol milter headers**: Milter `add_headers` and `remove_headers` exposed in the RSPAMC/SPAMC text protocol with extended symbol info including descriptions and options, enabling Exim to access milter headers via `$spam_report`. ([#5948](https://github.com/rspamd/rspamd/pull/5948))","**rspamadm new subcommands**: `rspamadm autolearnstats` for autolearn statistics analysis; `rspamadm logstats` and `rspamadm mapstats` as rewrites of legacy Perl scripts; `rspamadm statistics_dump migrate` for Bayes shard migration. ([#5946](https://github.com/rspamd/rspamd/pull/5946), [#5885](https://github.com/rspamd/rspamd/pull/5885), [#5914](https://github.com/rspamd/rspamd/pull/5914))","**HTTP content negotiation**: Framework for content negotiation on API endpoints; `/stat` endpoint supports zstd-compressed responses. ([#5832](https://github.com/rspamd/rspamd/pull/5832))","**PDF improvements**: ASCII85 decode support, ligature substitution fix, object padding evasion defeat, and small objects no longer counted toward processing limits. ([73a37be](https://github.com/rspamd/rspamd/commit/73a37be63), [eb1acde](https://github.com/rspamd/rspamd/commit/eb1acde80), [2b91e5e](https://github.com/rspamd/rspamd/commit/2b91e5ef5), [1f02010](https://github.com/rspamd/rspamd/commit/1f020105e))","**Reply-To validity checks**: New header checks for `Reply-To` address validity. ([e95533f](https://github.com/rspamd/rspamd/commit/e95533f1f))"]},{title:"Fixed",items:["**Fuzzy UDP use-after-free** (critical): Fixed use-after-free on ev_io watcher in fuzzy UDP sessions. ([4557166](https://github.com/rspamd/rspamd/commit/455716621))","**Fuzzy TCP CPU busy-loop**: Fixed CPU spin in fuzzy TCP client under certain error conditions. ([06dba44](https://github.com/rspamd/rspamd/commit/06dba4495))","**SPF address family flag inheritance**: Correct propagation of address family flags in SPF resolution. ([2a8643e](https://github.com/rspamd/rspamd/commit/2a8643e5e))","**DKIM RSA signing memory leak**: Fixed memory leak in RSA path of DKIM signing. ([9608160](https://github.com/rspamd/rspamd/commit/9608160b1))","**RHEL/CentOS 10 SHA-1 DKIM policy bypass**: Fixed crypto-policy bypass for SHA-1 DKIM signatures on RHEL/CentOS 10. ([7a38a8e](https://github.com/rspamd/rspamd/commit/7a38a8e33))","**Ratelimit compatibility with old records**: Fixed backward compatibility with legacy ratelimit bucket records. ([#5842](https://github.com/rspamd/rspamd/pull/5842))","**Weighted round-robin not respecting weights**: Fixed upstream selection ignoring configured weights. ([f563e25](https://github.com/rspamd/rspamd/commit/f563e25a0))","**SVG misdetection**: Fixed incorrect HTML detection for messages with embedded SVG content. ([170c4c5](https://github.com/rspamd/rspamd/commit/170c4c5d6))","**Hyperscan use-after-free on config reload**: Multiple use-after-free issues in Hyperscan cache handling during live configuration reload resolved. ([#5813](https://github.com/rspamd/rspamd/pull/5813))","**Jemalloc tuning**: Jemalloc tuned for Rspamd's single-threaded multi-process architecture, reducing memory overhead. ([#5949](https://github.com/rspamd/rspamd/pull/5949))"]},{title:"Improved",items:["**Consistent hash distribution**: Ring Hash with virtual nodes provides true minimal disruption on upstream failure and guarantees key return to original upstream on recovery, replacing the previous Jump Hash algorithm.","**Hyperscan async compilation**: Compilation no longer blocks the main event loop; self-healing blob detection ensures cache correctness after Hyperscan version changes.","**Fasttext memory efficiency**: Built-in shim shares model data across all worker processes via shared memory, eliminating 500MB\u20137GB of duplicate heap allocations typical in multi-worker deployments.","**Fuzzy hash expressiveness**: Multi-flag support allows a single stored digest to satisfy multiple independent rule checks simultaneously without duplication in storage."]}],summary:"Rspamd 4.0 is a landmark release delivering foundational infrastructure improvements alongside major new capabilities. The new `/checkv3` multipart protocol modernizes the scanning API with structured metadata, per-part compression, and zero-copy response paths. The built-in Fasttext shim eliminates a heavyweight C++ dependency while dramatically reducing per-worker memory usage. Multi-flag fuzzy hashes unlock more expressive detection rules, and HTML fuzzy phishing detection brings template-aware link-swap detection to the fuzzy engine. The move to Ring Hash consistent hashing corrects shard distribution behavior for Redis-backed deployments \u2014 users with sharded Bayes **must** run the migration tool before upgrading. This release is recommended for all users; users running sharded Redis Bayes backends should follow the migration procedure before upgrading."},{version:"3.14.3",date:"2026-01-08",type:"patch",title:"Patch Release with PDF Text Extraction, Task Registry, and ClickHouse Extensibility",sections:[{title:"Added",items:["**PDF text extraction**: Extract text from PDF attachments for content analysis with text quality filtering, FlateDecode and ASCIIHexDecode filter support, UTF-16 detection and conversion, ligature support for Standard and MacRoman encodings, and new `rspamadm mime -r` flag for raw extraction ([#5762](https://github.com/rspamd/rspamd/pull/5762))","**Task registry for Lua**: New task registry prevents use-after-free bugs in async Lua callbacks using counter-based keys instead of raw pointers for safe validation ([#5803](https://github.com/rspamd/rspamd/pull/5803))","**ClickHouse extra tables API**: Plugins can dynamically register custom ClickHouse tables via `rspamd_plugins['clickhouse'].register_extra_table()` with per-table schemas, row callbacks, and independent retention settings ([#5810](https://github.com/rspamd/rspamd/pull/5810))","**WebUI error log**: New error log modal tracking last 50 API errors with unseen error counter badge, copy-to-clipboard support with HTTP fallback, and color-coded error types with responsive column hiding ([#5821](https://github.com/rspamd/rspamd/pull/5821))"]},{title:"Fixed",items:["**Neural symbol flags**: Include symbols with no flags by default instead of excluding them","**Symcache FINE propagation**: Make FINE propagation deterministic ([#5825](https://github.com/rspamd/rspamd/issues/5825))","**URL numeric IP**: Prevent false positives from numeric IP regeneration in mailto URLs ([#5823](https://github.com/rspamd/rspamd/issues/5823))","**Settings selector**: Allow spaces in selector regexps","**Thai language**: Prevent R_MIXED_CHARSET false positives by marking Thai as having diacritics ([#5817](https://github.com/rspamd/rspamd/pull/5817), [#5799](https://github.com/rspamd/rspamd/issues/5799))","**GPT plugin**: Improved output format instructions for more reliable responses ([#5815](https://github.com/rspamd/rspamd/pull/5815), [#5670](https://github.com/rspamd/rspamd/issues/5670))","**io_uring HTTP**: Handle connection errors properly in HTTP client backend","**MIME Lua stack**: Restore Lua stack properly in second-pass detection","**Redis stats**: Add null checks for task in callbacks","**Lua 5.4 compatibility**: Fixes in ClickHouse and Elastic plugins ([#5774](https://github.com/rspamd/rspamd/pull/5774))","**Task lifetime crashes**: Fix potential crashes when Redis is unavailable longer than task lifetime ([#5803](https://github.com/rspamd/rspamd/pull/5803))"]},{title:"Improved",items:["**replxx build**: Fix build with LLVM 21+","**macOS build**: Add Homebrew LLVM libc++ library path","**fmt library**: Update to version 12.1.0","**SDK headers**: Avoid SDK headers in include path when package ROOT is specified"]}],summary:"This patch release introduces PDF text extraction capabilities for enhanced content analysis, improves stability through the new task registry that prevents use-after-free bugs in async Lua callbacks, and adds ClickHouse extensibility for custom table registration. Multiple bug fixes address neural network symbol handling, symcache determinism, URL processing, and language detection. Recommended upgrade for all users."},{version:"3.14.2",date:"2025-12-10",type:"patch",title:"Patch Release with Metadata Exporter Enhancements and DMARC RUA Validation",sections:[{title:"Added",items:["**Metadata Exporter Formatters**: New formatters `multipart`, `msgpack`, and `json_with_message` for better message content handling including binary support and multipart uploads","**DMARC RUA Validation**: New `rspamadm dmarc_report --recheck-rua` flag to re-validate cached report addresses against exclusion maps (useful with RBLs)","**Selective AR Header Removal**: New `remove_ar_from` setting in milter_headers to selectively remove Authentication-Results headers from specific domains"]},{title:"Improved",items:["**Metadata Exporter Docs**: Added comprehensive example for handling multipart metadata exports with Python/aiohttp"]},{title:"Deprecated",items:["**Metadata Exporter**: `meta_headers` option is deprecated; use `multipart` or `msgpack` formatters instead"]}],summary:""},{version:"3.14.1",date:"2025-12-01",type:"minor",title:"Minor Release with Composites Optimization, URL Deep Processing, and lua_shape Migration",sections:[{title:"Added",items:["**Composites optimization**: Inverted index with bloom filter for fast negative symbol lookups, precomputed atom types, and composites statistics with control protocol command ([#5764](https://github.com/rspamd/rspamd/pull/5764))","**Multimap combinators**: Support for combinator option in multimap selector rules for flexible matching logic ([#5766](https://github.com/rspamd/rspamd/pull/5766))","**SPF flattening tool**: New rspamadm utility for SPF record flattening with macro preservation ([#5757](https://github.com/rspamd/rspamd/pull/5757))","**URL deep processing**: Architecture for C-to-Lua URL filter consultation with async HTML URL processing ([#5748](https://github.com/rspamd/rspamd/pull/5748))","**Obfuscated URL detection**: Enhanced url_suspect plugin with rspamd_util.decode_html_entities for detecting obfuscated URLs ([#5761](https://github.com/rspamd/rspamd/pull/5761))","**lua_shape validation**: New validation library replacing tableshape with T.callable() type and callable defaults support ([#5754](https://github.com/rspamd/rspamd/pull/5754))","**Whitelist symbol flags**: Auto-mark whitelist symbols with SYMBOL_TYPE_FINE flag for better scoring granularity"]},{title:"Fixed",items:["**url_suspect false positives**: Fix plugin causing massive false positives with high URL volume messages ([#5758](https://github.com/rspamd/rspamd/pull/5758))","**Network chunking loop**: Prevent infinite loop in split_networks_into_chunks() function ([#5763](https://github.com/rspamd/rspamd/pull/5763))","**Memory leak**: Fix leak in custom tokenizer result handling","**lua_shape compatibility**: Multiple fixes for transform logic, registry resolution, error safety, and tableshape compatibility ([#5754](https://github.com/rspamd/rspamd/pull/5754))","**Composites inverted index**: Fix group matchers handling and improve atom polarity detection ([#5764](https://github.com/rspamd/rspamd/pull/5764))","**Lua composites**: Copy expression string to memory pool to prevent use-after-free ([#5764](https://github.com/rspamd/rspamd/pull/5764))","**D3 libraries**: Update with bug fixes and validation improvements ([#5765](https://github.com/rspamd/rspamd/pull/5765))","**Settings processing**: Keep groups_*/symbols_* fields in settings for runtime processing","**Redirect URL encoding**: Properly encode URLs with unencoded spaces and special characters ([#5748](https://github.com/rspamd/rspamd/pull/5748))","**WebUI hover**: Restore hover colors for symbols and fix hover behavior outside status tables ([#5741](https://github.com/rspamd/rspamd/pull/5741), [#5742](https://github.com/rspamd/rspamd/pull/5742))","**External relay**: Fix mixins and confighelp for external_relay module ([#5754](https://github.com/rspamd/rspamd/pull/5754))"]},{title:"Improved",items:["**url_suspect performance**: Optimize plugin for high URL volume messages with better deduplication ([#5758](https://github.com/rspamd/rspamd/pull/5758))","**tableshape migration**: Complete migration of all plugins and libraries from tableshape to lua_shape ([#5754](https://github.com/rspamd/rspamd/pull/5754))","**Configuration simplification**: Remove use_*_map flags and simplify URL processing configuration ([#5748](https://github.com/rspamd/rspamd/pull/5748))","**Whitelist maps**: Use contemporary API for maps in whitelist module ([#5746](https://github.com/rspamd/rspamd/pull/5746))","**WebUI symbols**: Improved hover colors and behavior for symbols display ([#5760](https://github.com/rspamd/rspamd/pull/5760))","**Plugins registry**: Centralized plugins registry with reworked mixins and documentation ([#5754](https://github.com/rspamd/rspamd/pull/5754))"]}],summary:"This minor release focuses on performance improvements and code modernization. The composites optimization with inverted index and bloom filter significantly speeds up composite rule evaluation. The new lua_shape library replaces the external tableshape dependency with a more integrated solution. URL processing has been enhanced with deep processing architecture and better obfuscated URL detection. Several critical bugs have been fixed including url_suspect false positives and an infinite loop in network processing."},{version:"3.14.0",date:"2025-11-10",type:"major",title:"Major Release with HTML Fuzzy Hashing, TCP Fuzzy Protocol, and Enhanced URL Processing",sections:[{title:"Added",items:["**HTML fuzzy hashing**: Structural similarity matching for HTML content with per-rule text_hashes toggle and dedicated shingles generation ([#5661](https://github.com/rspamd/rspamd/pull/5661), [#5720](https://github.com/rspamd/rspamd/pull/5720))","**Fuzzy TCP protocol**: Full TCP support in fuzzy storage and check with auto-switch, connection management, and proper framing ([#5669](https://github.com/rspamd/rspamd/pull/5669))","**CTA URL extraction**: New task:get_cta_urls() API and dedicated CTA module for proper call-to-action domain extraction ([#5732](https://github.com/rspamd/rspamd/pull/5732))","**Web search context**: LLM plugin integration with search API for domain context enrichment with Redis caching ([#5732](https://github.com/rspamd/rspamd/pull/5732))","**HTML URL rewriting**: Infrastructure for async HTML URL rewriting with Lua bindings and UTF-8 support ([#5676](https://github.com/rspamd/rspamd/pull/5676))","**Dark mode**: Full dark mode implementation in WebUI with theme toggle and auto detection ([#5725](https://github.com/rspamd/rspamd/pull/5725))","**Email aliases**: Advanced aliases resolution with loop detection for converging paths and expand_multiple mode ([#5655](https://github.com/rspamd/rspamd/pull/5655))","**ESMTP arguments**: Milter ESMTP argument parsing with Lua API access for per-recipient metadata ([#5663](https://github.com/rspamd/rspamd/pull/5663))","**URL hash method**: Exposed url:get_hash() method for efficient deduplication without string conversion overhead ([#5732](https://github.com/rspamd/rspamd/pull/5732))","**Postfix wizard**: Configwizard integration for automated Postfix setup using postconf utility ([#5667](https://github.com/rspamd/rspamd/pull/5667))","**BSD workflows**: Comprehensive GitHub Actions workflows for FreeBSD, NetBSD, and OpenBSD with Lua version selection ([#5726](https://github.com/rspamd/rspamd/pull/5726), [#5728](https://github.com/rspamd/rspamd/pull/5728), [#5729](https://github.com/rspamd/rspamd/pull/5729))","**Automated code review**: GitHub Actions workflow for Droid-powered code review with Rspamd-specific guidelines ([#5732](https://github.com/rspamd/rspamd/pull/5732))","**Multimap multisymbol**: Support for symbols with leading numerals in multimap plugin ([#5680](https://github.com/rspamd/rspamd/pull/5680))","**Remove headers array**: Support array of positions for milter remove_headers operations ([#5673](https://github.com/rspamd/rspamd/pull/5673))","**Client IP forwarding**: Proxy client IP preservation in message headers through chain ([#5671](https://github.com/rspamd/rspamd/pull/5671))","**Milter header support**: rspamc --mime support for milter.add_headers object format ([#5684](https://github.com/rspamd/rspamd/pull/5684))","**Public suffix automation**: Automatic synchronization of public suffix list via GitHub Actions ([#5718](https://github.com/rspamd/rspamd/pull/5718))","**Fuzzy migration utility**: Redis migration utility for fuzzy storage data ([#5692](https://github.com/rspamd/rspamd/pull/5692))","**Integration test suite**: Docker-based integration tests with ASAN, leak detection, and real corpus ([#5688](https://github.com/rspamd/rspamd/pull/5688))","**DMARC report headers**: Auto-Reply-To and Precedence headers to prevent out-of-office replies ([#5686](https://github.com/rspamd/rspamd/pull/5686))","**NetBSD memory support**: Memory usage tracking for NetBSD platform ([#5726](https://github.com/rspamd/rspamd/pull/5726))"]},{title:"Fixed",items:["**DNS truncation**: Preserve req->pos during reply validation to prevent packet truncation on UDP-to-TCP retransmits ([#5739](https://github.com/rspamd/rspamd/pull/5739))","**DNS transaction ID**: Regenerate transaction ID before copying to TCP buffer to avoid collisions ([#5739](https://github.com/rspamd/rspamd/pull/5739))","**DMARC report batching**: Add batching and forced GC for Redis connections to prevent connection pool exhaustion ([#5737](https://github.com/rspamd/rspamd/pull/5737))","**Batch size validation**: Validate and normalize batch_size to prevent fractional indexing and loop errors ([#5737](https://github.com/rspamd/rspamd/pull/5737))","**Allocator mismatches**: Fix jemalloc/system malloc mixing in getline(), hiredis, and libucl to prevent crashes ([#5721](https://github.com/rspamd/rspamd/pull/5721), [#5724](https://github.com/rspamd/rspamd/pull/5724))","**Hyperscan version**: Use runtime version instead of compile-time for database validation and auto-recreate invalid cache files ([#5724](https://github.com/rspamd/rspamd/pull/5724))","**DNS round-robin**: Fix nameserver rotation from /etc/resolv.conf using ROUND_ROBIN instead of MASTER_SLAVE ([#5721](https://github.com/rspamd/rspamd/pull/5721))","**Memory leaks**: Fix leaks in fuzzy storage khash, upstream parsing, address parsing, OpenSSL providers, and UCL objects ([#5709](https://github.com/rspamd/rspamd/pull/5709))","**Fuzzy TCP bugs**: Fix double-release, timeout handling, buffer overflow, endianness mismatch, and race conditions ([#5669](https://github.com/rspamd/rspamd/pull/5669), [#5716](https://github.com/rspamd/rspamd/pull/5716))","**Shutdown tracking**: Keep srv events active during shutdown to track auxiliary processes via pipe notifications ([#5728](https://github.com/rspamd/rspamd/pull/5728))","**ARC signing**: Restore strict header ordering per RFC 8617 and add ed25519 key support ([#5684](https://github.com/rspamd/rspamd/pull/5684))","**Composite evaluation**: Implement two-phase evaluation for postfilter dependencies and fix symbol lookup ([#5681](https://github.com/rspamd/rspamd/pull/5681))","**URL extraction DoS**: Refactor to use hash-based deduplication with 50k URL limit to prevent string table exhaustion ([#5732](https://github.com/rspamd/rspamd/pull/5732))","**Bayes autolearn**: Allow skipping local/authenticated mail in default autolearn condition ([#5679](https://github.com/rspamd/rspamd/pull/5679))","**Bayes Redis discovery**: Improve Redis server discovery for Bayes storage ([#5714](https://github.com/rspamd/rspamd/pull/5714))","**ESMTP args parsing**: Robust per-recipient parsing in milter with safe cursor advance and refcount management ([#5663](https://github.com/rspamd/rspamd/pull/5663))","**HTML attribute offsets**: Correct offset calculation for URL rewriting in HTML attributes ([#5676](https://github.com/rspamd/rspamd/pull/5676))","**OpenBSD support**: Fix kinfo_proc member names and disable Hyperscan on OpenBSD ([#5729](https://github.com/rspamd/rspamd/pull/5729))","**FreeBSD packages**: Fix zstd package name and add IGNORE_OSVERSION for version mismatches ([#5729](https://github.com/rspamd/rspamd/pull/5729))","**NetBSD build**: Fix package installation with pkgin and correct dependency names ([#5726](https://github.com/rspamd/rspamd/pull/5726), [#5729](https://github.com/rspamd/rspamd/pull/5729))","**Bayes metadata leak**: Fix memory leak in stat metadata tokenization ([#5688](https://github.com/rspamd/rspamd/pull/5688))","**MIME anonymization**: Remove Authentication-Results and anonymize envelope-from in Received headers for LLM processing ([#5687](https://github.com/rspamd/rspamd/pull/5687))","**Multimap symbols**: Handle symbols with leading numerals in multimap ([#5680](https://github.com/rspamd/rspamd/pull/5680))","**Aliases validation**: Prevent creation of malformed email addresses in aliases module ([#5655](https://github.com/rspamd/rspamd/pull/5655))","**HTML fuzzy cache**: Fix cache key collision between text and HTML fuzzy hashes ([#5661](https://github.com/rspamd/rspamd/pull/5661))","**CSS normalization**: Fix CSS class normalization in HTML fuzzy token generation ([#5661](https://github.com/rspamd/rspamd/pull/5661))"]},{title:"Improved",items:["**RBL configuration**: Refactored with new from selectors, content_urls checks, and lower_utf8 for hashed domains ([#5733](https://github.com/rspamd/rspamd/pull/5733))","**URL prioritization**: Prioritize CTA URLs in redirector and Lua helpers with proper phishing bonus preservation ([#5732](https://github.com/rspamd/rspamd/pull/5732))","**Fuzzy checks structure**: New structured checks configuration with backward-compatible legacy flags support ([#5720](https://github.com/rspamd/rspamd/pull/5720))","**Hash performance**: Replace GHashTable with khash in fuzzy_check and CTA URL extraction for better performance ([#5720](https://github.com/rspamd/rspamd/pull/5720), [#5732](https://github.com/rspamd/rspamd/pull/5732))","**WebUI icons**: Replace Glyphicons with FontAwesome SVG icons ([#5702](https://github.com/rspamd/rspamd/pull/5702))","**WebUI libraries**: Update CodeJar to 4.3.0, Node.js/ESLint, and D3-based visualization libs ([#5684](https://github.com/rspamd/rspamd/pull/5684), [#5717](https://github.com/rspamd/rspamd/pull/5717), [#5738](https://github.com/rspamd/rspamd/pull/5738))","**Bayes learn guards**: Make learn guards configurable instead of hardcoded ([#5701](https://github.com/rspamd/rspamd/pull/5701))","**Lua logger**: Add type specifiers support for better formatting ([#5668](https://github.com/rspamd/rspamd/pull/5668))","**DMARC reporting**: Refactor to use helper functions and async maps for better maintainability ([#5722](https://github.com/rspamd/rspamd/pull/5722))","**Config refcounting**: Add CFG_REF_* macros with debug logging for better lifecycle tracking ([#5709](https://github.com/rspamd/rspamd/pull/5709))","**Memory pool destructors**: Smart destructor preallocation based on pool type with specialized allocation strategies ([#5693](https://github.com/rspamd/rspamd/pull/5693))","**Heap implementation**: Convert to fully intrusive kvec-based implementation eliminating double allocation ([#5693](https://github.com/rspamd/rspamd/pull/5693))","**OpenSSL lifecycle**: Move providers from global to libs_ctx for clearer ownership ([#5709](https://github.com/rspamd/rspamd/pull/5709))","**Libucl stack management**: Automatic stack preservation for included files without outer braces ([#5717](https://github.com/rspamd/rspamd/pull/5717))","**Body rewriting**: Improve body rewriting support in rspamc and proxy ([#5675](https://github.com/rspamd/rspamd/pull/5675))","**AI assistant config**: Add Claude Code and Cursor AI configuration for development ([#5667](https://github.com/rspamd/rspamd/pull/5667))","**Fuzzy logging**: Add protocol logging and reduce dumb log verbosity ([#5669](https://github.com/rspamd/rspamd/pull/5669), [#5701](https://github.com/rspamd/rspamd/pull/5701))","**Docker configuration**: Add explicit console logging and runtime flags ([#5701](https://github.com/rspamd/rspamd/pull/5701))"]}],summary:"This major release introduces groundbreaking features including HTML fuzzy hashing for structural similarity detection, TCP protocol support for fuzzy storage with improved reliability, and advanced URL processing with CTA extraction and deduplication. The release includes comprehensive memory leak fixes, improved BSD platform support, enhanced WebUI with dark mode, and better integration capabilities through ESMTP argument parsing and Postfix wizard. Critical DNS fixes prevent packet truncation, while DMARC reporting improvements solve Redis connection exhaustion. This is a significant upgrade recommended for all users, especially those processing HTML-heavy email or requiring advanced fuzzy matching capabilities."},{version:"3.13.2",date:"2025-10-05",type:"minor",title:"Feature Update with Enhanced Security and Performance",sections:[{title:"Added",items:["**Fuzzy check encryption**: Separate encryption keys for read and write operations in fuzzy_check plugin ([#5665](https://github.com/rspamd/rspamd/pull/5665))","**DKIM ED25519 support**: Full ED25519 support for DKIM signing and verification with OpenSSL version checks ([#5664](https://github.com/rspamd/rspamd/pull/5664))","**Vault KV v2 support**: HashiCorp Vault KV version 2 support for DKIM key management with backward compatibility ([#5654](https://github.com/rspamd/rspamd/pull/5654))","**MetaDefender integration**: MetaDefender Cloud Lua module for SHA256 hash lookups as free-tier anti-malware scanning alternative ([#5656](https://github.com/rspamd/rspamd/pull/5656))","**LLM context support**: User/domain context support for LLM-based classification with Redis-based conversation context ([#5647](https://github.com/rspamd/rspamd/pull/5647))","**DMARC RUA exclusion**: Configuration option to exclude specific RUA addresses from DMARC report storage ([#5653](https://github.com/rspamd/rspamd/pull/5653))"]},{title:"Fixed",items:["**DKIM bodyhash calculation**: Fixed relaxed bodyhash calculation for lines with only spaces to comply with RFC 6376 Section 3.4.4 ([#5662](https://github.com/rspamd/rspamd/pull/5662))","**DKIM key loading**: Fixed ED25519 key loading to prevent memory corruption in union handling ([#5664](https://github.com/rspamd/rspamd/pull/5664))","**HTTP map intervals**: Enforced server-controlled refresh intervals and prevented aggressive polling behavior ([#5660](https://github.com/rspamd/rspamd/pull/5660))","**HTTP map overflow**: Prevented time_t overflow in expires header processing ([#5660](https://github.com/rspamd/rspamd/pull/5660))","**Once received plugin**: Fixed duplicate symbol addition by changing break to return in check_quantity_received ([#5658](https://github.com/rspamd/rspamd/pull/5658))","**Redis Sentinel**: Properly propagate unused Sentinel options ([#5597](https://github.com/rspamd/rspamd/pull/5597))","**Fuzzy check decryption**: Fixed reply decryption when using separate read/write keys ([#5665](https://github.com/rspamd/rspamd/pull/5665))","**Fuzzy check fallback**: Added fallback when only one specific encryption key is set ([#5665](https://github.com/rspamd/rspamd/pull/5665))","**Fuzzy check filtering**: Fixed duplicate key filtering in reply decryption ([#5665](https://github.com/rspamd/rspamd/pull/5665))","**Fuzzy ping servers**: Allow read/write servers configuration ([#5665](https://github.com/rspamd/rspamd/pull/5665))"]},{title:"Improved",items:["**Fuzzy check performance**: Refactored encryption key selection into helper functions for better maintainability ([#5665](https://github.com/rspamd/rspamd/pull/5665))","**Fuzzy check efficiency**: Stop early when found a correct key to improve performance ([#5665](https://github.com/rspamd/rspamd/pull/5665))","**Development workflow**: Added cursor rules for improved development experience ([#5665](https://github.com/rspamd/rspamd/pull/5665))"]}],summary:"This release introduces significant security enhancements with separate encryption keys for fuzzy operations, modern cryptographic support with ED25519 DKIM keys, improved infrastructure integration with Vault KV v2, and enhanced anti-malware capabilities with MetaDefender. The release also includes important fixes for DKIM compliance, HTTP map behavior, and various stability improvements. This is recommended as a security and feature update."},{version:"3.13.1",date:"2025-09-30",type:"minor",title:"Feature Update with Stability and Security Improvements",sections:[{title:"Added",items:["**Archive module**: Full support for encrypted ZIP archives, including both ZipCrypto and AES encryption; both reading and writing of AES-encrypted ZIP archives is supported with updated Lua bindings using libarchive for flexible compatibility with all standard ZIP encryption schemes","**Encrypted maps**: Support for encrypted maps to enable new map distribution scenarios","**Redis TLS**: Configurable TLS connections in Redis backend for improved compatibility in secure environments"]},{title:"Improved",items:["**MIME encoding refactoring**: Major overhauls and multiple fixes for MIME encoding logic, including improved handling and decoding of UTF-8 in MIME headers, resulting in more robust email processing and better compatibility","**Learning system**: Numerous fixes to learn checks and autolearn flag handling, prevention of duplicate message learning, and extended multiclass learning test coverage","**Map helpers alignment**: Map helpers now enforced to be aligned to 64 bytes to prevent unaligned memory access errors on certain platforms","**CLI enhancements**: Enhanced secretbox CLI and additional security test coverage","**Platform compatibility**: Improved compatibility with Lua versions above 5.1 and better support for 32-bit platforms"]},{title:"Fixed",items:["**Critical fixes**: Fixed bug when converting zero-length strings to numbers","**XML parsing**: Fixed XML prolog detection in lua_magic module","**Build issues**: Fixed build issues on 32-bit platforms","**Empty input handling**: Addressed issues with empty input handling in lua_magic","**Test stability**: Improved stability of automated testing with multiple miscellaneous test fixes","**Compatibility**: Minor compatibility improvements and bugfixes (buffer allocation, missing cmath include, etc.)"]}],summary:"This release introduces archive module extensibility with full encrypted archive support including AES, new map distribution capabilities, secure integration options with Redis TLS, robust email and message processing improvements, and bugfixes for broader platform compatibility. This is recommended as a major stability and feature update."},{version:"3.13.0",date:"2025-09-17",type:"major",title:"Major Release with Breaking Changes",sections:[{title:"Breaking Changes",items:["Multiclass Bayes classification with named classes (2\u201320 classes), processed in a single Redis call per message. ([#5547](https://github.com/rspamd/rspamd/pull/5547))","Backward compatible with `is_spam`; new configuration supports labeled classes and autolearn.","Lua API and autolearn support multiclass workflows with class\u2011aware Redis caching.","CLI: `rspamc learn_class:transactional \u2026`, `rspamc learn_class:newsletter \u2026`.","Lua API: `task:get_multiclass_result()` returns class probabilities and confidence."]},{title:"Added",items:["Neural module overhaul to a provider\u2011based architecture (symbols, LLM embeddings), versioned and backward compatible. ([#5579](https://github.com/rspamd/rspamd/pull/5579))","Pluggable feature fusion with trained normalization (unit/zscore/none) applied at train and inference.","Redis\u2011backed caching for LLM embeddings to control cost and latency.","Multimap selectors: SA\u2011style selectors with dedicated `selector` field; integrated with Hyperscan and regex cache. ([#5615](https://github.com/rspamd/rspamd/pull/5615))","MIME parser automatically detects part types. ([#5619](https://github.com/rspamd/rspamd/pull/5619), [#5608](https://github.com/rspamd/rspamd/pull/5608))","HTML parser extracts more features for downstream modules."]},{title:"Improved",items:["HTTP timeouts configuration and handling. ([#5614](https://github.com/rspamd/rspamd/pull/5614), [#5603](https://github.com/rspamd/rspamd/pull/5603), [#5601](https://github.com/rspamd/rspamd/pull/5601))","Upstream reliability with probe mode; reduced need for forced revive.","DNS nameserver resolution switched to `getaddrinfo`.","Standardized on C++20; build, test, and CI improvements (including ARM support and modern fallback maps). ([#5592](https://github.com/rspamd/rspamd/pull/5592), [#5598](https://github.com/rspamd/rspamd/pull/5598), [#5580](https://github.com/rspamd/rspamd/pull/5580))","Regular code cleaning and RPM tweaks.","WebUI: E2E scan test flows; Bootstrap upgrade; Bayes class management from the web interface. ([#5606](https://github.com/rspamd/rspamd/pull/5606), [#5607](https://github.com/rspamd/rspamd/pull/5607))","GPT/LLM integrations: improved parameter/prompt handling; initial support for OpenAI GPT\u20115 and other models. ([#5612](https://github.com/rspamd/rspamd/pull/5612), [#5572](https://github.com/rspamd/rspamd/pull/5572))"]},{title:"Fixed",items:["DCC plugin rewritten and optimized. ([#5602](https://github.com/rspamd/rspamd/pull/5602))","DKIM relaxed body canonicalization; improved reliability in multimap, WebUI, and configuration. ([#5593](https://github.com/rspamd/rspamd/pull/5593))","Numerous minor bug fixes and CI/build improvements."]}],summary:""},{version:"3.12.1",date:"2025-06-17",type:"minor",title:"Minor Release with Bug Fixes and Improvements",sections:[{title:"Added",items:["**Bayes Classifiers Endpoint**: Add `/bayes/classifiers` HTTP endpoint for better classifier management","**Enhanced Scheduling**: Further improvements in scheduling next checks for better performance","**AEAD Testing**: Add comprehensive tests for AEAD (Authenticated Encryption with Associated Data) used in Rspamd","**Improved Maps Consistency**: Better consistency in cached flag for maps and logging when dealing with Expires"]},{title:"Improved",items:["**Maps Locking**: Eliminate maps locking for better performance and reduced contention","**Proxy Headers**: Fix proxy headers duplication and improve header handling","**Milter Logic**: Do not add log tag header in milter logic for cleaner output","**Connection Handling**: Do not explicitly add Connection header if it's already present","**Attachment Processing**: Treat *.library-ms and *.search-ms attachments as harmful (with proper application)","**Maps Concurrent Loading**: Fix race conditions in maps concurrent loading","**Write Operations**: Try to avoid incomplete writes for better data integrity"]},{title:"Fixed",items:["**Maps Locking Issues**: Eliminate maps locking to prevent deadlocks and improve performance","**Proxy Headers Duplication**: Fix proxy headers duplication issue","**Milter Header Logic**: Fix logic error in milter_headers.lua: skip_wanted() function","**Connection Header**: Prevent duplicate Connection headers in proxy","**Maps Concurrent Access**: Fix race conditions in maps concurrent loading","**Incomplete Writes**: Prevent incomplete writes that could cause data corruption","**Attachment Security**: Properly apply security rules for *.library-ms and *.search-ms attachments","**Logging Consistency**: Improve consistency in logging when dealing with Expires and cached flags"]},{title:"Changed",items:["**Maps Architecture**: Eliminate maps locking mechanism for better performance","**Header Processing**: Improve header processing to avoid duplicates","**Attachment Handling**: Enhanced security for Windows library and search attachments","**Scheduling Logic**: Improved scheduling algorithm for next checks","**Logging Format**: Better consistency in logging output and cached flag handling"]},{title:"Security",items:["**Attachment Security**: Enhanced protection against potentially harmful Windows library and search attachments","**Header Validation**: Improved header validation to prevent duplication and injection","**Write Protection**: Better protection against incomplete writes that could lead to data corruption"]},{title:"Performance",items:["**Maps Performance**: Eliminate maps locking for better concurrent access performance","**Scheduling**: Improved scheduling algorithm for better resource utilization","**Header Processing**: Optimized header processing to reduce overhead"]},{title:"Testing",items:["**AEAD Testing**: Comprehensive tests for AEAD encryption used throughout Rspamd","**Maps Testing**: Improved testing for maps concurrent loading scenarios","**Header Testing**: Better testing for header processing and duplication scenarios"]}],summary:"This minor release focuses on stability improvements, bug fixes, and performance enhancements. The elimination of maps locking provides significant performance improvements for high-concurrency environments, while the enhanced attachment security and header processing improvements make Rspamd more robust and secure."},{version:"3.12.0",date:"2025-06-10",type:"major",title:"Major Release with Enhanced Features and Security Improvements",sections:[{title:"Added",items:["**GPT Module Enhancements**: Add Ollama support and token usage logging for OpenAI-compatible APIs","**Fuzzy Storage Improvements**: Add support for separate read and write servers in fuzzy check","**CDB Maps Support**: Allow CDB files as external maps for improved performance","**Contextal Integration**: New plugin to integrate with Contextal platform for advanced threat detection","**Enhanced Logging**: Allow to specify max log tag length for all log messages and log tag in proxy","**Proxy Improvements**: Add keep-alive support, HTTPS backend connections, and extra headers specification","**Redis Version Support**: Allow to specify Redis version for compatibility","**HEIC File Support**: Add HEIC files patterns for better image analysis","**Enhanced Maps Management**: Show all maps status and output content for all maps","**File Upload in WebUI**: Add file upload functionality to Test Selectors","**Convenience Methods**: Add various convenience methods for Lua development","**Enhanced Fuzzy Operations**: Add deletion of specific fuzzy hashes in WebUI","**Improved Error Handling**: Better error messages for multimap invalid types"]},{title:"Improved",items:["**Security Enhancements**: Critical fix to disable macros and file variables by default in lua-ucl","**Maps Loading**: Fix race condition in maps loading by unlocking backend on switch","**Greylisting**: Improve body hash calculations for better accuracy","**Replies Module**: Rework to consider all recipients and use SMTP ones","**WebUI Updates**: Update Bootstrap, D3 and PrismJS libraries to latest versions","**Lua Logging**: Major improvements to logging output format and complex table key support","**Fuzzy Storage**: Better handling of fuzzy lua callback when there are no shingles","**Milter Headers**: Fix logic error in skip_wanted() function","**Known Senders**: Improved recipients test logic and consistency with replies module","**Maps Management**: Grey out not loaded maps in the Maps table for better visibility","**Redis Integration**: Improve Redis script loading and caching framework usage","**Configuration**: Use safe parsers everywhere except configuration for security","**Build System**: Modernize cmake and improve sanitizers support","**Documentation**: Update API docs for multiple HTTP headers and various other improvements"]},{title:"Fixed",items:["**Critical Security**: Disable macros and file variables by default in lua-ucl parser","**Race Conditions**: Fix race condition in maps loading by unlocking backend on switch","**Memory Issues**: Fix lua-bit stack buffer overflow vulnerability","**Proxy Crashes**: Prevent crashes when accessing upstream address in self-scan mode","**Maps Management**: Fix maps IDs and static maps description passing","**Fuzzy Storage**: Filter invalid domains in fuzzy extra data","**Redis Integration**: Fix various Redis-related issues and improve script loading","**WebUI Issues**: Fix Fuzzy hashes card close button alignment and map editor modal handling","**Build Issues**: Fix Debian package build and RPM log directory attributes","**Compiler Warnings**: Fix various compile warnings and compatibility issues","**Test Framework**: Fix various test issues and improve test configuration","**Documentation**: Fix writing rules tutorial link and various documentation issues","**URL Lists**: Update default URL for openphish and fix various URL-related issues","**Headers Processing**: Fix header processing in various modules","**Lua Integration**: Fix various Lua-related issues and improve error handling"]},{title:"Changed",items:["**Configuration**: Use safe parsers everywhere except configuration for enhanced security","**Maps Architecture**: Rework to use locks/loaded per backend for all maps","**Replies Logic**: Consider all recipients and use SMTP ones instead of just From header","**Fuzzy Storage**: Remove servers completely and use `read_servers` for all compatibility","**Logging Format**: Improve format string processing and logging output","**Build System**: Modernize cmake and rework OSDep for better maintainability","**WebUI**: Rework file upload JS implementation and improve user interface","**Documentation**: Update various documentation files and improve API documentation","**Error Handling**: Improve error messages and handling throughout the codebase","**Performance**: Various performance improvements in maps, Redis, and fuzzy operations"]},{title:"Removed",items:["**Deprecated Features**: Remove various deprecated and unused code paths","**Unused Helpers**: Remove unused cleanTextUpload helper and other obsolete code","**Legacy Support**: Remove compatibility code that is no longer needed"]},{title:"Security",items:["**Critical Fix**: Disable macros and file variables by default in lua-ucl parser","**Memory Safety**: Fix lua-bit stack buffer overflow vulnerability","**Input Validation**: Improve input validation and error handling throughout","**Configuration Security**: Use safe parsers everywhere except configuration"]},{title:"Performance",items:["**Maps Loading**: Optimize maps loading with better caching and race condition fixes","**Redis Operations**: Improve Redis script loading and caching framework usage","**Fuzzy Storage**: Better handling of fuzzy operations and storage management","**Memory Usage**: Various memory optimization improvements"]},{title:"Documentation",items:["**API Documentation**: Update API docs for multiple HTTP headers and various modules","**Tutorial Links**: Fix writing rules tutorial link and other documentation references","**Examples**: Add more examples for Lua HTTP and other modules","**Configuration**: Improve configuration documentation and examples"]},{title:"Testing",items:["**Test Framework**: Fix various test issues and improve test configuration","**Fuzzy Tests**: Add comprehensive tests for split, read-only, and write-only server modes","**CDB Maps**: Add tests for CDB maps functionality","**HEIC Recognition**: Add tests for HEIC file recognition"]}],summary:"This release includes significant security improvements, enhanced functionality, and better performance across all components of Rspamd. The addition of Contextal integration, improved fuzzy storage, and enhanced WebUI features make this a major release with substantial improvements for users."},{version:"3.11.1",date:"2024-12-15",type:"minor",title:"Feature Update with Bug Fixes",sections:[{title:"Added",items:["GPT: Add ollama support","Allow to hash any Lua types","Allow to use LLM for anonymize","Add ability to not send response_format in gpt plugin to support gpt4all","Allow to store shingles as opaque Lua data","Add 'noop' redis backend for scripts running","Allow multiple lua scripts for fuzzy storage","Support LLM models consensus","GPT: Support reason adding","Add Redis caching framework","Add ability to create timers from Lua"]},{title:"Improved",items:["Expand Detection of Fake Reply Subjects Across Multiple Languages","Add another acceptable mime type for icon","Respect ipv4 and ipv6 configurations for rbl resolve_ip","Set RBL checks to bool true","Rules regexp url separated and fix no subdomain cases for Google urls","WebUI: Reset dropdown when clearing filters","Pass both the multimap and the rules descriptions for combined multimap on create","Some small fixes to statistics_dump","More features to GPT plugin","Better support for maps and IP-related fixes/improvements in settings","Use caching framework in gpt module","Try to check maps earlier if their expires is too long"]},{title:"Fixed",items:["Fix transposed results in `rspamadm fuzzy_ping`","connIp is not correctly added to request","Fix Thunderbird for Android marked with FORGED_MUA_THUNDERBIRD_MSGID_UNKNOWN","Fix issue with synchronous Redis calls","Fix some broken links","rbl check_types was missing images","RBL: fix use of `content_urls` and `images` inside `checks`","Use sub_utf8 to strip headers value to not break utf8 strings","Properly close multipart/related boundary when adding text footer","Verify key type to match DKIM signature type","Avoid collision hacks in mempool variables hash","Add expiration for neural ham and spam sets","Properly expire neural ham and spam sets"]},{title:"Changed",items:["Log queue id with cloudmark analysis string","Allow to disable RBLs via map","Prevent option duplicates in rspamd_stats.pl","Regenerate manpages with recent Pandoc version","Fix spelling errors in libserver","Update JavaScript linters"]},{title:"Removed",items:["Remove nixspam"]}],summary:""},{version:"3.11.0",date:"2024-12-01",type:"major",title:"Major Release with Breaking Changes",sections:[{title:"Breaking Changes",items:["**Elasticsearch/OpenSearch Plugin**: Major rework with breaking changes","Added support for Elasticsearch 8 & OpenSearch 2","Added index policy with logs retention","Updated configuration format"]},{title:"Added",items:["Added LRU cache for last filled ratelimit buckets","Added utilities to manage ratelimit buckets","Added include/exclude logic for headers processing","Added `rspamadm mime strip` command for attachments removal","Added new message anonymization capabilities","Added more ways to extend Rspamd configuration, including `lua.local.d` folder"]},{title:"Improved",items:["Improved address rotation algorithm for upstream selection","Replaced fastutf with simdutf for better architecture support and performance","Reworked symbol description display on hover in WebUI","Improved keyboard accessibility in WebUI","Enhanced symbol rendering in WebUI","Improved handling of DNS limits in SPF module","Improved GPT module JSON parsing","Multiple performance optimizations"]},{title:"Fixed",items:["Fixed ARC-Seal signing issues","Fixed RFC 2047 header encoding","Fixed issues with dynamic keys in fuzzy storage","Fixed TCP connection handling with cumulative timeouts","Fixed multiple phishing detection false positives","Fixed DMARC structured headers encoding"]},{title:"Changed",items:["Skip extra RBL checks when Received IP matches From IP","Multimap now uses only distinct text parts for content matching","Various configuration and logging improvements"]}],summary:""}];function W(e,t){for(var a=e.version.split(".").map(function(e){return parseInt(e,10)}),s=t.version.split(".").map(function(e){return parseInt(e,10)}),r=0;r<3;r++){if(a[r]>s[r])return-1;if(a[r]<s[r])return 1}return 0}function G(e){switch(e){case"major":return{color:"#e74c3c",icon:"\ud83d\ude80",label:"Major Release",description:"New features, breaking changes"};case"minor":return{color:"#3498db",icon:"\u2728",label:"Feature Release",description:"New features, backward compatible"};case"patch":return{color:"#27ae60",icon:"\ud83d\udc1b",label:"Bug Fix Release",description:"Bug fixes, security patches"};default:return{color:"#95a5a6",icon:"\ud83d\udce6",label:"Release",description:"General release"}}}function K(){var e=(0,s.useState)("all"),t=e[0],a=e[1],r=[].concat(O).sort(W).filter(function(e){return"all"===t||e.type===t});return(0,z.jsxs)("div",{className:F,children:[(0,z.jsxs)("div",{className:D,children:[(0,z.jsx)("h1",{children:"\ud83d\udccb Changelog"}),(0,z.jsx)("p",{className:E,children:"All notable changes to this project are documented here."}),(0,z.jsx)("div",{className:U,children:(0,z.jsx)(I,{})})]}),(0,z.jsx)("div",{className:N,children:["all","major","minor","patch"].map(function(e){return(0,z.jsx)("button",{className:H+" "+(t===e?j:""),onClick:function(){return a(e)},children:"all"===e?"\ud83d\udce6 All Releases":G(e).icon+" "+G(e).label},e)})}),(0,z.jsx)("div",{className:B,children:r.map(function(e){return(0,z.jsx)(C,{release:e,typeInfo:G(e.type)},e.version)})}),0===r.length&&(0,z.jsx)("div",{className:q,children:"No releases found for the selected filter."})]})}function V(){return(0,z.jsx)(r.A,{title:"Changelog",description:"View all changes and releases for this project",children:(0,z.jsx)(K,{})})}}}]);